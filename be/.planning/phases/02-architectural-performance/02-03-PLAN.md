---
phase: "02-architectural-performance"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Application/Options/AnalyticsOptions.cs",
  "src/Syncra.Application/Services/IntegrationAnalyticsService.cs",
  "src/Syncra.Infrastructure/DependencyInjection.cs",
  "src/Syncra.Api/appsettings.json"
]
autonomous: true
requirements: ["TASK-2.3"]
must_haves:
  truths:
    - "Analytics services use IOptions<AnalyticsOptions> for configuration"
    - "Cache TTL for analytics is configurable via appsettings.json"
    - "IntegrationAnalyticsService no longer calls Environment.GetEnvironmentVariable directly"
  artifacts:
    - path: "src/Syncra.Application/Options/AnalyticsOptions.cs"
      provides: "Structured configuration for analytics"
  key_links:
    - from: "src/Syncra.Application/Services/IntegrationAnalyticsService.cs"
      to: "AnalyticsOptions"
      via: "IOptions injection"
---

<objective>
Implement `AnalyticsOptions` and decouple analytics services from direct environment variable checks.

Purpose: Improve testability and configurability by following the standard .NET Options pattern.
Output: `AnalyticsOptions` class, updated `IntegrationAnalyticsService` with injected options, and configuration binding in DI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Syncra.Application/Services/IntegrationAnalyticsService.cs
@src/Syncra.Infrastructure/DependencyInjection.cs

<interfaces>
From IntegrationAnalyticsService.cs (Current hardcoded logic):
```csharp
private static readonly TimeSpan CacheTtl = string.Equals(
    Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT"), "Development",
    StringComparison.OrdinalIgnoreCase)
    ? TimeSpan.FromSeconds(1)
    : TimeSpan.FromHours(1);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalyticsOptions class</name>
  <files>src/Syncra.Application/Options/AnalyticsOptions.cs</files>
  <action>
    Create `src/Syncra.Application/Options/AnalyticsOptions.cs`.
    Define properties:
    - `public const string SectionName = "Analytics";`
    - `public int CacheTtlSeconds { get; set; } = 3600;` (Default to 1 hour)
    - Add a helper property `public TimeSpan CacheTtl => TimeSpan.FromSeconds(CacheTtlSeconds);`
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>AnalyticsOptions class is defined.</done>
</task>

<task type="auto">
  <name>Task 2: Inject AnalyticsOptions into IntegrationAnalyticsService</name>
  <files>src/Syncra.Application/Services/IntegrationAnalyticsService.cs</files>
  <action>
    1. Update constructor to inject `IOptions<AnalyticsOptions>`.
    2. Replace the static `CacheTtl` field with a private field initialized from options: `_options.Value.CacheTtl`.
    3. Remove `Environment.GetEnvironmentVariable` usage.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>IntegrationAnalyticsService uses Options pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Bind AnalyticsOptions in DependencyInjection</name>
  <files>src/Syncra.Infrastructure/DependencyInjection.cs</files>
  <action>
    Add `services.Configure<AnalyticsOptions>(configuration.GetSection(AnalyticsOptions.SectionName));` to the `AddInfrastructureServices` method.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>AnalyticsOptions are bound in DI.</done>
</task>

<task type="auto">
  <name>Task 4: Update appsettings.json</name>
  <files>src/Syncra.Api/appsettings.json, src/Syncra.Api/appsettings.Development.json</files>
  <action>
    Add the `Analytics` section to `appsettings.json` with `CacheTtlSeconds: 3600`.
    Add the `Analytics` section to `appsettings.Development.json` with `CacheTtlSeconds: 1`.
  </action>
  <verify>
    Check if the settings are present in files.
  </verify>
  <done>Configuration files updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| appsettings -> Options | Configuration values from untrusted sources (if overridden by env vars in prod) are mapped to Options. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | AnalyticsOptions | mitigate | Ensure `CacheTtlSeconds` has a reasonable minimum value (e.g., via validation or in-code check) to prevent extremely high load if set to 0. |
</threat_model>

<verification>
1. Run `dotnet test tests/Syncra.UnitTests` to ensure everything still works.
2. (Optional) Create a small unit test to verify `IntegrationAnalyticsService` uses the TTL from options.
</verification>

<success_criteria>
- Analytics services are fully decoupled from environment variables.
- Cache TTL is configurable per environment.
- Code follows the standard .NET Options pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architectural-performance/02-03-SUMMARY.md`
</output>
