---
phase: "02-architectural-performance"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Domain/Common/Result.cs",
  "src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs",
  "src/Syncra.Application/Interfaces/IWorkspaceAnalyticsService.cs",
  "src/Syncra.Application/Services/IntegrationAnalyticsService.cs",
  "src/Syncra.Application/Services/WorkspaceAnalyticsService.cs"
]
autonomous: true
requirements: ["TASK-2.2"]
must_haves:
  truths:
    - "Analytics services return Result<T> instead of raw objects or empty lists for errors"
    - "Result<T> correctly captures success/failure state and error messages"
    - "WorkspaceAnalyticsService handles failures from IntegrationAnalyticsService gracefully"
  artifacts:
    - path: "src/Syncra.Domain/Common/Result.cs"
      provides: "Unified result pattern for error handling"
  key_links:
    - from: "src/Syncra.Application/Services/WorkspaceAnalyticsService.cs"
      to: "IIntegrationAnalyticsService"
      via: "Method calls returning Result<T>"
---

<objective>
Implement `Result<T>` pattern and refactor analytics services to use it for better error handling and observability.

Purpose: Replace "silent" failures (returning empty lists) with explicit error states, improving debugging and API error reporting.
Output: Custom `Result<T>` type and refactored `IIntegrationAnalyticsService` and `IWorkspaceAnalyticsService`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs
@src/Syncra.Application/Services/IntegrationAnalyticsService.cs

<interfaces>
Current IIntegrationAnalyticsService:
```csharp
public interface IIntegrationAnalyticsService
{
    Task<List<AnalyticsData>> CheckAnalyticsAsync(Guid workspaceId, Guid integrationId, int date, CancellationToken cancellationToken = default);
    Task<List<AnalyticsData>> CheckPostAnalyticsAsync(Guid workspaceId, Guid postId, int date, CancellationToken cancellationToken = default);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Result&lt;T&gt; in Syncra.Domain</name>
  <files>src/Syncra.Domain/Common/Result.cs</files>
  <action>
    Create `src/Syncra.Domain/Common/Result.cs`.
    Implement a lightweight `Result<T>` and `Result` class with:
    - `IsSuccess` (bool)
    - `IsFailure` (bool)
    - `Value` (T, only for Result&lt;T&gt;)
    - `Error` (string or Error object)
    - Static factory methods: `Success()`, `Success<T>(T value)`, `Failure(string error)`, `Failure<T>(string error)`.
    Include implicit operators if helpful.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Domain</automated>
  </verify>
  <done>Result type is implemented and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor IIntegrationAnalyticsService to use Result&lt;T&gt;</name>
  <files>src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs, src/Syncra.Application/Services/IntegrationAnalyticsService.cs</files>
  <action>
    1. Update `IIntegrationAnalyticsService` interface:
       - Change `Task<List<AnalyticsData>>` to `Task<Result<List<AnalyticsData>>>`.
    2. Update `IntegrationAnalyticsService` implementation:
       - Wrap successful returns in `Result.Success(...)`.
       - Replace `return new List<AnalyticsData>()` with `Result.Failure<List<AnalyticsData>>("...")` providing descriptive error messages (e.g., "Integration not found", "Token refresh failed").
       - Ensure all catch blocks return `Result.Failure`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>IntegrationAnalyticsService refactored and returns Result&lt;T&gt;.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor IWorkspaceAnalyticsService to use Result&lt;T&gt;</name>
  <files>src/Syncra.Application/Interfaces/IWorkspaceAnalyticsService.cs, src/Syncra.Application/Services/WorkspaceAnalyticsService.cs</files>
  <action>
    1. Update `IWorkspaceAnalyticsService` interface:
       - Change return types to `Task<Result<WorkspaceAnalyticsSummaryDto>>` and `Task<Result<HeatmapDto>>`.
    2. Update `WorkspaceAnalyticsService` implementation:
       - Update `GetSummaryAsync` to handle `Result` from `CheckAnalyticsAsync`.
       - If some integrations fail but others succeed, decide on a policy (e.g., partial success with warning, or fail fast). Per D-03, we should probably return success if we have some data, but log failures.
       - Wrap final results in `Result.Success`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>WorkspaceAnalyticsService refactored and returns Result&lt;T&gt;.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application -> Domain | Application services use Domain Result type to convey operational outcome. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Result.Error | mitigate | Ensure internal exception details (stack traces, sensitive paths) are not leaked into the `Error` property if it's returned to the UI. Use generic error messages for end-users. |
</threat_model>

<verification>
1. Run `dotnet test tests/Syncra.UnitTests` to ensure no regressions.
2. Verify that unit tests for analytics services (if they exist) are updated to check for `IsSuccess`.
</verification>

<success_criteria>
- All analytics service methods return `Result<T>`.
- Error messages are meaningful and logged.
- Codebase no longer uses "empty list" as a proxy for error states in analytics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architectural-performance/02-02-SUMMARY.md`
</output>
