---
phase: "02-architectural-performance"
plan: "02"
type: execute
wave: 1
status: completed
depends_on: []
files_modified: [
  "src/Syncra.Domain/Common/Result.cs",
  "src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs",
  "src/Syncra.Application/Interfaces/IWorkspaceAnalyticsService.cs",
  "src/Syncra.Application/Services/IntegrationAnalyticsService.cs",
  "src/Syncra.Application/Services/WorkspaceAnalyticsService.cs",
  "src/Syncra.Api/Common/ResultExtensions.cs",
  "src/Syncra.Application/Features/Analytics/Queries/GetIntegrationAnalyticsQueryHandler.cs",
  "src/Syncra.Application/Features/Analytics/Queries/GetWorkspaceSummaryQueryHandler.cs",
  "src/Syncra.Application/Features/Analytics/Queries/GetPostAnalyticsQueryHandler.cs",
  "src/Syncra.Application/Features/Analytics/Queries/GetWorkspaceHeatmapQueryHandler.cs"
]
autonomous: true
requirements: ["TASK-2.2"]
must_haves:
  truths:
    - "Analytics services return Result<T> instead of raw objects or empty lists for errors"
    - "Result<T> correctly captures success/failure state and error messages"
    - "WorkspaceAnalyticsService handles failures from IntegrationAnalyticsService gracefully"
    - "API controllers can easily map Result<T> to IActionResult using an extension method"
  artifacts:
    - path: "src/Syncra.Domain/Common/Result.cs"
      provides: "Unified result pattern for error handling"
    - path: "src/Syncra.Api/Common/ResultExtensions.cs"
      provides: "Result to ActionResult mapping"
  key_links:
    - from: "src/Syncra.Application/Services/WorkspaceAnalyticsService.cs"
      to: "IIntegrationAnalyticsService"
      via: "Method calls returning Result<T>"
---

<objective>
Implement `Result<T>` pattern and refactor analytics services and handlers to use it for better error handling and observability.

Purpose: Replace "silent" failures (returning empty lists) with explicit error states, improving debugging and API error reporting.
Output: Custom `Result<T>` type, refactored analytics services, and `ToActionResult` mapping extension.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs
@src/Syncra.Application/Services/IntegrationAnalyticsService.cs

<interfaces>
Current IIntegrationAnalyticsService:
```csharp
public interface IIntegrationAnalyticsService
{
    Task<List<AnalyticsData>> CheckAnalyticsAsync(Guid workspaceId, Guid integrationId, int date, CancellationToken cancellationToken = default);
    Task<List<AnalyticsData>> CheckPostAnalyticsAsync(Guid workspaceId, Guid postId, int date, CancellationToken cancellationToken = default);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Result&lt;T&gt; in Syncra.Domain</name>
  <files>src/Syncra.Domain/Common/Result.cs</files>
  <action>
    Create `src/Syncra.Domain/Common/Result.cs`.
    Implement a lightweight `Result<T>` and `Result` class with:
    - `IsSuccess` (bool)
    - `IsFailure` (bool)
    - `Value` (T, only for Result&lt;T&gt;)
    - `Error` (string or Error object)
    - Static factory methods: `Success()`, `Success<T>(T value)`, `Failure(string error)`, `Failure<T>(string error)`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Domain</automated>
  </verify>
  <done>Result type is implemented and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ToActionResult extension method</name>
  <files>src/Syncra.Api/Common/ResultExtensions.cs</files>
  <action>
    Create `src/Syncra.Api/Common/ResultExtensions.cs`.
    Implement `ToActionResult<T>(this Result<T> result)` that:
    - Returns `Ok(result.Value)` if success.
    - Returns `BadRequest(result.Error)` (or appropriate status based on error type) if failure.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Api</automated>
  </verify>
  <done>ToActionResult extension is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Analytics Services to use Result&lt;T&gt;</name>
  <files>src/Syncra.Application/Interfaces/IIntegrationAnalyticsService.cs, src/Syncra.Application/Services/IntegrationAnalyticsService.cs, src/Syncra.Application/Interfaces/IWorkspaceAnalyticsService.cs, src/Syncra.Application/Services/WorkspaceAnalyticsService.cs</files>
  <action>
    Update both `IIntegrationAnalyticsService` and `IWorkspaceAnalyticsService` and their implementations to return `Result<T>`.
    - Replace empty list returns with `Result.Failure`.
    - Wrap successful data in `Result.Success`.
    - Handle nested `Result` calls in `WorkspaceAnalyticsService`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Analytics services refactored and return Result&lt;T&gt;.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor MediatR Query Handlers to handle Result&lt;T&gt;</name>
  <files>src/Syncra.Application/Features/Analytics/Queries/GetIntegrationAnalyticsQueryHandler.cs, src/Syncra.Application/Features/Analytics/Queries/GetWorkspaceSummaryQueryHandler.cs, src/Syncra.Application/Features/Analytics/Queries/GetPostAnalyticsQueryHandler.cs, src/Syncra.Application/Features/Analytics/Queries/GetWorkspaceHeatmapQueryHandler.cs</files>
  <action>
    Update the `Handle` methods of the specified query handlers:
    1. Change return type to `Task<Result<TResponse>>`.
    2. Propagate the `Result` from the underlying service call.
    3. Ensure `IRequest` interfaces for these queries are also updated to `IRequest<Result<TResponse>>`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>MediatR query handlers refactored to use Result&lt;T&gt;.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application -> Domain | Application services use Domain Result type to convey operational outcome. |
| API -> Application | API maps Result to IActionResult. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Result.Error | mitigate | Ensure internal exception details are not leaked into the `Error` property returned via `ToActionResult`. |
</threat_model>

<verification>
1. Run `dotnet test tests/Syncra.UnitTests` to ensure no regressions.
2. Verify that unit tests for analytics services and handlers are updated.
</verification>

<success_criteria>
- All analytics service methods and query handlers return `Result<T>`.
- `ToActionResult` successfully maps results in controllers.
- Error messages are meaningful and logged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architectural-performance/02-02-SUMMARY.md`
</output>
