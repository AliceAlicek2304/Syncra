---
phase: "02-architectural-performance"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Domain/Entities/Plan.cs",
  "src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs",
  "src/Syncra.Infrastructure/Persistence/Seed/PlanSeedData.cs"
]
autonomous: true
requirements: ["TASK-2.4"]
must_haves:
  truths:
    - "Plan entity includes StripePriceId and StripeProductId fields"
    - "Database schema matches the updated Plan entity"
    - "Seed data contains real or placeholder Stripe IDs for all plans"
  artifacts:
    - path: "src/Syncra.Infrastructure/Migrations/*_UpdatePlanWithStripeIds.cs"
      provides: "Schema update for Plans table"
  key_links:
    - from: "src/Syncra.Domain/Entities/Plan.cs"
      to: "database"
      via: "EF Core migration"
---

<objective>
Update `Plan` entity, configuration, and seed data with Stripe identifiers to decouple subscription logic from internal GUIDs.

Purpose: Allow the system to look up internal plans using external Stripe identifiers (Price ID, Product ID), which is essential for handling Stripe webhooks and checkout sessions.
Output: Updated `Plan` entity, EF Core configuration, seed data, and a new database migration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Syncra.Domain/Entities/Plan.cs
@src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs
@src/Syncra.Infrastructure/Persistence/Seed/PlanSeedData.cs

<interfaces>
Current Plan entity:
```csharp
public sealed class Plan : EntityBase
{
    public string Code { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
    // ... other properties ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stripe fields to Plan entity</name>
  <files>src/Syncra.Domain/Entities/Plan.cs</files>
  <action>
    Add the following properties to `src/Syncra.Domain/Entities/Plan.cs`:
    - `public string? StripeProductId { get; set; }`
    - `public string? StripePriceId { get; set; }`
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Domain</automated>
  </verify>
  <done>Plan entity updated with Stripe fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update Plan configuration</name>
  <files>src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs</files>
  <action>
    Modify `PlanConfiguration` in `SubscriptionConfigurations.cs`:
    - Add mapping for `StripeProductId` (max length 100, column name `stripe_product_id`).
    - Add mapping for `StripePriceId` (max length 100, column name `stripe_price_id`).
    - Add a unique index on `StripePriceId`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>Plan configuration updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update Plan seed data</name>
  <files>src/Syncra.Infrastructure/Persistence/Seed/PlanSeedData.cs</files>
  <action>
    Update `PlanSeedData.cs` to include placeholder Stripe IDs for each plan (e.g., `prod_placeholder_free`, `price_placeholder_free`).
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>Seed data updated.</done>
</task>

<task type="auto">
  <name>Task 4: Generate and apply migration</name>
  <files>src/Syncra.Infrastructure/Migrations/</files>
  <action>
    1. Run `dotnet ef migrations add UpdatePlanWithStripeIds --project src/Syncra.Infrastructure --startup-project src/Syncra.Api`.
    2. (Optional) Apply migration locally: `dotnet ef database update --project src/Syncra.Infrastructure --startup-project src/Syncra.Api`.
  </action>
  <verify>
    <automated>ls src/Syncra.Infrastructure/Migrations/*UpdatePlanWithStripeIds.cs</automated>
  </verify>
  <done>Migration generated and applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain -> Persistence | Schema changes reflect domain model updates. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | Plan table | accept | Stripe IDs are public identifiers, no risk in storing them in the DB. |
</threat_model>

<verification>
1. Verify the migration file content.
2. Verify the database schema (if possible) or run the project to ensure seed data is applied.
</verification>

<success_criteria>
- Plan table has `stripe_product_id` and `stripe_price_id` columns.
- Existing plans have seeded Stripe IDs.
- Subscriptions can later be looked up via `StripePriceId`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architectural-performance/02-04-SUMMARY.md`
</output>
