---
phase: 01-security-tenancy-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/Syncra.Infrastructure/Services/StripeService.cs]
autonomous: true
requirements: [1.2]
must_haves:
  truths:
    - "StripeService does not modify global StripeConfiguration.ApiKey"
    - "All Stripe API calls in StripeService pass RequestOptions with the API key"
  artifacts:
    - path: "src/Syncra.Infrastructure/Services/StripeService.cs"
      provides: "Stateless Stripe service"
---

<objective>
Refactor StripeService to eliminate global state and use local request options, following best practices for multi-tenant or multi-environment reliability.

Purpose: Prevent accidental API key leaks or overlaps in a multi-environment context.
Output: Cleaned `StripeService` using per-request configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@src/Syncra.Infrastructure/Services/StripeService.cs
@src/Syncra.Application/Options/StripeOptions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 0: Refactor StripeService to use RequestOptions</name>
  <files>src/Syncra.Infrastructure/Services/StripeService.cs</files>
  <action>
    Modify `StripeService` per D-03:
    - Remove the line `StripeConfiguration.ApiKey = _stripeOptions.SecretKey;` from the constructor.
    - Create a private helper method or property `GetRequestOptions()` that returns a `new RequestOptions { ApiKey = _stripeOptions.SecretKey }`.
    - Update every Stripe service call (e.g., `customerService.GetAsync`, `customerService2.CreateAsync`, `sessionService.CreateAsync`, `service.CreateAsync`) to include the `RequestOptions` as an argument.
    - Ensure all calls remain asynchronous.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure/Syncra.Infrastructure.csproj</automated>
  </verify>
  <done>StripeService is refactored to use local request options.</done>
</task>

<task type="auto">
  <name>Task 1: Verify StripeService functionality</name>
  <files>tests/Syncra.UnitTests/Infrastructure/StripeServiceTests.cs</files>
  <action>
    Update or create tests for `StripeService` to ensure it still functions correctly after the refactor.
    - Since it's hard to test actual Stripe calls without a key, verify that the services are instantiated and called with the correct parameters (using mocks if services are injectable, otherwise focus on compilation and unit logic).
    - Note: If Stripe SDK services are not easily mockable, verify via smoke tests if a test key is available, or rely on existing integration tests if any.
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter "FullyQualifiedName~StripeService"</automated>
  </verify>
  <done>Refactored StripeService passes existing unit tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | StripeService | mitigate | Removing global state prevents cross-tenant/environment key usage if multiple keys were ever used. |
</threat_model>

<success_criteria>
Compilation succeeds and no reference to `StripeConfiguration.ApiKey` remains in the constructor.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-tenancy-hardening/01-03-SUMMARY.md`
</output>
