---
phase: 01-security-tenancy-hardening
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [src/Syncra.Api/Controllers/StripeWebhookController.cs]
autonomous: true
requirements: [1.1]
must_haves:
  truths:
    - "Stripe events are only processed once even if sent multiple times"
    - "Processed events return 200 OK without re-triggering logic"
  artifacts:
    - path: "src/Syncra.Api/Controllers/StripeWebhookController.cs"
      provides: "Idempotent webhook handling"
  key_links:
    - from: "src/Syncra.Api/Controllers/StripeWebhookController.cs"
      to: "Syncra.Domain.Entities.IdempotencyRecord"
      via: "AppDbContext"
---

<objective>
Implement idempotency for Stripe Webhooks to ensure each event is processed exactly once, protecting the billing pipeline from duplicate operations.

Purpose: Prevent duplicate subscription updates or cancellations due to webhook retries.
Output: Hardened `StripeWebhookController` with event deduplication.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@src/Syncra.Api/Controllers/StripeWebhookController.cs
@src/Syncra.Domain/Entities/IdempotencyRecord.cs
@src/Syncra.Infrastructure/Persistence/AppDbContext.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Implement Webhook Idempotency</name>
  <files>src/Syncra.Api/Controllers/StripeWebhookController.cs, tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs</files>
  <behavior>
    - When an event with ID 'evt_123' is received for the first time:
        1. Create `IdempotencyRecord` with key 'stripe_event_evt_123' and status 'Pending'.
        2. Process the event.
        3. Update record status to 'Success'.
        4. Return 200 OK.
    - When an event with ID 'evt_123' is received again (duplicate):
        1. Return 200 OK immediately without re-processing logic.
  </behavior>
  <action>
    Modify `StripeWebhookController.Index` per D-01:
    - Inject `AppDbContext`.
    - Extract `stripeEvent.Id`.
    - Check `db.IdempotencyRecords` for key `$"stripe_event_{stripeEvent.Id}"`.
    - If exists, log and return `Ok()`.
    - If not exists, create `IdempotencyRecord`, `await db.SaveChangesAsync()`.
    - Wrap event processing (the switch statement) in a try-catch.
    - On success, update record status to `Success`.
    - On failure, update record status to `Failed` (or delete if retry is desired).
    - Ensure `await db.SaveChangesAsync()` is called after update.
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter "FullyQualifiedName~StripeWebhookControllerTests"</automated>
  </verify>
  <done>Stripe webhooks are idempotent based on Event ID.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | StripeWebhookController | mitigate | Idempotency prevents processing cost of duplicate events |
</threat_model>

<success_criteria>
Tests confirm that sending the same Stripe Event ID twice only triggers the underlying command once.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-tenancy-hardening/01-02-SUMMARY.md`
</output>
