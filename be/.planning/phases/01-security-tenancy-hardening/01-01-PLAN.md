---
phase: 01-security-tenancy-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs, tests/Syncra.UnitTests/Api/Middleware/TenantResolutionMiddlewareTests.cs]
autonomous: true
requirements: [1.4]
must_haves:
  truths:
    - "Integration tests for Stripe webhooks exist and pass"
    - "Integration tests for TenantResolutionMiddleware exist and pass"
  artifacts:
    - path: "tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs"
      provides: "Stripe webhook integration tests"
    - path: "tests/Syncra.UnitTests/Api/Middleware/TenantResolutionMiddlewareTests.cs"
      provides: "Tenant resolution integration tests"
  key_links:
    - from: "tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs"
      to: "src/Syncra.Api/Controllers/StripeWebhookController.cs"
      via: "WebApplicationFactory"
---

<objective>
Establish baseline integration tests for critical security components (Stripe Webhooks and Tenant Resolution) to enable verified hardening in subsequent waves.

Purpose: Ensure reliability and prevent regressions during security refactoring.
Output: Two new integration test files providing baseline coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@src/Syncra.Api/Controllers/StripeWebhookController.cs
@src/Syncra.Api/Middleware/TenantResolutionMiddleware.cs
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Stripe Webhook baseline tests</name>
  <files>tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs</files>
  <action>
    Create an integration test for `StripeWebhookController` using `WebApplicationFactory`.
    - Mock `IMediator` to verify commands are sent.
    - Test `checkout.session.completed` event: verify it triggers `UpdateSubscriptionCommand`.
    - Test `customer.subscription.deleted` event: verify it triggers `CancelSubscriptionCommand`.
    - Use a valid-looking Stripe signature (can be mocked if `EventUtility` is wrapped or signature verification is bypassed for tests).
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter "FullyQualifiedName~StripeWebhookControllerTests"</automated>
  </verify>
  <done>Baseline tests for Stripe webhooks pass.</done>
</task>

<task type="auto">
  <name>Task 1: Create Tenant Resolution baseline tests</name>
  <files>tests/Syncra.UnitTests/Api/Middleware/TenantResolutionMiddlewareTests.cs</files>
  <action>
    Create integration tests for `TenantResolutionMiddleware`.
    - Test success case: valid header + valid membership -> 200 (or pass through to dummy endpoint).
    - Test failure case: valid header + NO membership -> 403 Forbidden.
    - Test failure case: malformed header -> 400 Bad Request.
    - Test bypass case: missing header -> pass through.
    Use `WebApplicationFactory` and a test DB (SQLite in-memory or similar if already configured).
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter "FullyQualifiedName~TenantResolutionMiddlewareTests"</automated>
  </verify>
  <done>Baseline tests for Tenant resolution pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External -> StripeWebhookController | Untrusted Stripe events |
| External -> API (Header) | Untrusted tenant ID in header |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | StripeWebhookController | mitigate | Signature verification (existing) + Idempotency (Wave 1) |
| T-01-02 | Information Disclosure | TenantResolutionMiddleware | mitigate | Validate membership before allowing access (existing) + Cache hardening (Wave 3) |
</threat_model>

<success_criteria>
Integration tests exist for both components and confirm current (baseline) behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-tenancy-hardening/01-01-SUMMARY.md`
</output>
