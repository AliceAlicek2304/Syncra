---
phase: "03-quality-observability"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [
  "tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs",
  "tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs"
]
autonomous: true
requirements: ["TASK-3.2"]
must_haves:
  truths:
    - "AnalyticsController has comprehensive unit tests"
    - "StripeWebhookController has comprehensive unit tests"
    - "All tests pass with mocked dependencies"
  artifacts:
    - path: "tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs"
      provides: "Analytics controller test coverage"
---

<objective>
Complete test suite for Analytics and Stripe controllers.

Purpose: Ensure critical controller logic is thoroughly tested with mocked dependencies.
Output: Unit test files with comprehensive coverage of controller actions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalyticsControllerTests</name>
  <files>tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs</files>
  <action>
    Create unit tests for AnalyticsController covering:
    1. GetAnalytics returns cached data when available
    2. GetAnalytics calls service and caches result when not cached
    3. GetAnalytics returns 404 when integration not found
    4. GetPostAnalytics validates workspace ownership
    5. Error responses use Result pattern correctly
  </action>
  <done>AnalyticsController tests created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create StripeWebhookControllerTests</name>
  <files>tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs</files>
  <action>
    Create unit tests for StripeWebhookController covering:
    1. Webhook processes valid events successfully
    2. Webhook rejects invalid signatures
    3. Idempotency prevents duplicate processing
    4. Different event types handled correctly
    5. Error responses are properly formatted
  </action>
  <done>StripeWebhookController tests created and passing.</done>
</task>

</tasks>

<verification>
1. Run `dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~AnalyticsControllerTests`
2. Run `dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~StripeWebhookControllerTests`
3. All tests pass
</verification>

<success_criteria>
- AnalyticsController has >80% test coverage
- StripeWebhookController has >80% test coverage
- All controller actions have corresponding tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-observability/03-02-SUMMARY.md`
</output>
