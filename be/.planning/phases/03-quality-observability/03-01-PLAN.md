---
phase: "03-quality-observability"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Application/Interfaces/IIntegrationHealthService.cs",
  "src/Syncra.Application/Services/IntegrationHealthService.cs",
  "src/Syncra.Infrastructure/Jobs/IntegrationHealthCheckJob.cs",
  "src/Syncra.Api/Controllers/HealthController.cs"
]
autonomous: true
requirements: ["TASK-3.1"]
must_haves:
  truths:
    - "Integration health status can be queried via API"
    - "Expired/Expiring tokens are detected and logged"
    - "Health check job runs periodically"
  artifacts:
    - path: "src/Syncra.Application/Services/IntegrationHealthService.cs"
      provides: "OAuth health monitoring logic"
---

<objective>
Implement OAuth health tracking and auditing for integrations.

Purpose: Monitor OAuth token health, detect expiring tokens, and provide visibility into integration status.
Output: Health check service, background job, and API endpoint for integration health status.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create IIntegrationHealthService interface</name>
  <files>src/Syncra.Application/Interfaces/IIntegrationHealthService.cs</files>
  <action>
    Create interface with methods:
    - Task<IntegrationHealthResult> CheckHealthAsync(Guid integrationId)
    - Task<List<IntegrationHealthSummary>> GetAllHealthAsync(Guid workspaceId)
    - Define IntegrationHealthResult and IntegrationHealthSummary DTOs
  </action>
  <done>Interface defined with health check methods.</done>
</task>

<task type="auto">
  <name>Task 2: Implement IntegrationHealthService</name>
  <files>src/Syncra.Application/Services/IntegrationHealthService.cs</files>
  <action>
    Implement service that:
    1. Queries integrations and checks token expiration
    2. Returns health status (Healthy, ExpiringSoon, Expired)
    3. Logs health check results for auditing
  </action>
  <done>Service implementation complete with health logic.</done>
</task>

<task type="auto">
  <name>Task 3: Create IntegrationHealthCheckJob</name>
  <files>src/Syncra.Infrastructure/Jobs/IntegrationHealthCheckJob.cs</files>
  <action>
    Create Hangfire job that:
    1. Runs periodically (configurable interval)
    2. Checks all integrations for token health
    3. Logs warnings for expiring/expired tokens
    4. Optional: Send notifications for critical issues
  </action>
  <done>Background job implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 4: Add HealthController endpoint</name>
  <files>src/Syncra.Api/Controllers/HealthController.cs</files>
  <action>
    Add endpoint GET /api/health/integrations that:
    1. Returns health status for all workspace integrations
    2. Supports filtering by status (Healthy, ExpiringSoon, Expired)
    3. Requires workspace authentication
  </action>
  <done>API endpoint exposed for health queries.</done>
</task>

</tasks>

<verification>
1. Build passes: `dotnet build`
2. Health check returns correct status for test integrations
3. Background job runs without errors
</verification>

<success_criteria>
- Integration health can be queried via API
- Expiring tokens are detected and logged
- Health check job runs on schedule
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-observability/03-01-SUMMARY.md`
</output>
