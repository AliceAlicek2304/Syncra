---
phase: 01-security-tenancy-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/Syncra.Infrastructure/Services/StripeService.cs]
autonomous: true
requirements: [1.2]
must_haves:
  truths:
    - "StripeService does not modify global StripeConfiguration.ApiKey"
    - "All Stripe API calls in StripeService pass RequestOptions with the API key"
  artifacts:
    - path: "src/Syncra.Infrastructure/Services/StripeService.cs"
      provides: "Stateless Stripe service"
---

<objective>
Refactor StripeService to eliminate global state and use local request options, using the baseline unit tests from Wave 0 to ensure zero regression.

Purpose: Prevent accidental API key leaks or overlaps in a multi-environment context.
Output: Cleaned `StripeService` using per-request configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@src/Syncra.Infrastructure/Services/StripeService.cs
@src/Syncra.Application/Options/StripeOptions.cs
@tests/Syncra.UnitTests/Infrastructure/StripeServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 0: Refactor StripeService to use RequestOptions</name>
  <files>src/Syncra.Infrastructure/Services/StripeService.cs</files>
  <action>
    Modify `StripeService` per D-03:
    - Remove `StripeConfiguration.ApiKey = _stripeOptions.SecretKey;` from constructor.
    - Implement `private RequestOptions GetRequestOptions()` helper.
    - Update all Stripe SDK service calls to pass `GetRequestOptions()`.
    - Ensure methods `GetOrCreateCustomerAsync` and `CreateCheckoutSessionAsync` are updated.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure/Syncra.Infrastructure.csproj</automated>
  </verify>
  <done>StripeService is refactored to use local request options.</done>
</task>

<task type="auto">
  <name>Task 1: Verify refactor against baseline tests</name>
  <files>tests/Syncra.UnitTests/Infrastructure/StripeServiceTests.cs</files>
  <action>
    Run the unit tests created in Phase 01-01 to ensure the refactor didn't break core functionality.
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter "FullyQualifiedName~StripeServiceTests"</automated>
  </verify>
  <done>Refactored StripeService passes all baseline tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | StripeService | mitigate | Removing global state prevents cross-tenant/environment key usage. |
</threat_model>

<success_criteria>
Compilation succeeds and `StripeServiceTests` pass without `StripeConfiguration.ApiKey` being set.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-tenancy-hardening/01-03-SUMMARY.md`
</output>
