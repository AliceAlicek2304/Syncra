---
phase: 7
plan: 4
type: execute
wave: 3
depends_on: [1, 3]
files_modified:
  - .planning/phases/07-billing-ux-documentation/07-BILLING-MODULE.md
  - .planning/phases/07-billing-ux-documentation/07-UAT.md
  - .planning/phases/07-billing-ux-documentation/07-OPS-RUNBOOK.md
autonomous: true
requirements:
  - REQ-4.1
  - REQ-5.1
---

<objective>
Ship Phase 7 documentation artifacts under `.planning/phases/07-billing-ux-documentation/`:
- Billing module overview + endpoint contract notes
- Frontend UX notes + manual UAT checklist (happy + failure paths)
- Ops runbook for billing troubleshooting

This satisfies Phase decisions D-16/D-17 and milestone requirement 5.1.
</objective>

<threat_model>
## Threats

### T1: Operational blind spots (can’t debug billing incidents)
- **Severity:** MEDIUM
- **Mitigation:** Provide an ops runbook with concrete checks (logs, Stripe dashboard, DB tables).

### T2: Misuse of tenant header leading to data leaks
- **Severity:** HIGH
- **Mitigation:** Document that all billing calls must send `X-Workspace-Id` and match route param.
</threat_model>

<tasks>

<task id="4.1" type="execute">
<title>Create Billing module overview + endpoints doc</title>
<read_first>
- be/.planning/phases/07-CONTEXT.md
- be/.planning/phases/07-billing-ux-documentation/07-RESEARCH.md
- be/src/Syncra.Api/Controllers/SubscriptionsController.cs
- be/src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs
</read_first>
<files>
- .planning/phases/07-billing-ux-documentation/07-BILLING-MODULE.md
</files>
<action>
Create `.planning/phases/07-billing-ux-documentation/07-BILLING-MODULE.md` documenting:

- Purpose + scope (Settings → Billing)
- **Backend endpoints** (include method + route + body/response shapes):
  - `POST /api/v2/workspaces/{workspaceId}/subscription/create-checkout-session`
  - `POST /api/v1/workspaces/{workspaceId}/subscription/create-portal-session` (or v2 if added)
  - `GET /api/v1/workspaces/{workspaceId}/subscription`
- Required headers:
  - `Authorization: Bearer <token>`
  - `X-Workspace-Id: <workspaceId>`
  - Note: header must match route param.
- Plan code casing requirement (FREE/PRO/TEAM).
- Return query param contract: `?billing=success|cancel|portal_return`.

Keep it concise and implementation-oriented.
</action>
<acceptance_criteria>
- `07-BILLING-MODULE.md` contains `/api/v2/workspaces/{workspaceId}/subscription/create-checkout-session`
- `07-BILLING-MODULE.md` contains `X-Workspace-Id`
- `07-BILLING-MODULE.md` contains `?billing=success|cancel|portal_return`
- `07-BILLING-MODULE.md` contains `FREE` and `PRO` and `TEAM`
</acceptance_criteria>
</task>

<task id="4.2" type="execute">
<title>Create manual UAT checklist for Billing UX (happy + failure paths)</title>
<read_first>
- be/.planning/phases/07-CONTEXT.md (D-12..D-15)
- .planning/phases/06-webhook-reliability-idempotency/06-UAT.md (format reference)
</read_first>
<files>
- .planning/phases/07-billing-ux-documentation/07-UAT.md
</files>
<action>
Create `.planning/phases/07-billing-ux-documentation/07-UAT.md` with a checklist that includes:

**Setup**
- How to set `syncra_workspace_id` in browser localStorage for FE testing
- How to set `syncra_access_token` (if required)
- Reminder: send `X-Workspace-Id` header

**Happy path**
- Owner opens Settings → Billing
- Starts checkout for PRO monthly
- Returns with `billing=success` and sees auto-refresh
- Opens portal and returns with `billing=portal_return`

**Failure paths (must include at least these):**
- Checkout canceled (`billing=cancel`)
- Network/API error creating checkout session
- Portal session creation fails
- Stale subscription state after return (confirm refetch occurs)

Add a small “Expected result” section for each test.
</action>
<acceptance_criteria>
- `07-UAT.md` contains `billing=cancel`
- `07-UAT.md` contains `billing=success`
- `07-UAT.md` contains `billing=portal_return`
- `07-UAT.md` contains `syncra_workspace_id`
- `07-UAT.md` contains `syncra_access_token`
</acceptance_criteria>
</task>

<task id="4.3" type="execute">
<title>Create ops runbook for billing troubleshooting</title>
<read_first>
- be/.planning/phases/06-webhook-reliability-idempotency/06-RESEARCH.md (webhook reliability context)
- be/src/Syncra.Api/Controllers/PaymentsWebhookController.cs
- be/src/Syncra.Api/Controllers/StripeWebhookController.cs
- be/src/Syncra.Domain/Entities/IdempotencyRecord.cs
</read_first>
<files>
- .planning/phases/07-billing-ux-documentation/07-OPS-RUNBOOK.md
</files>
<action>
Create `.planning/phases/07-billing-ux-documentation/07-OPS-RUNBOOK.md` with:

- Common issues + how to diagnose:
  - Checkout session creation fails (invalid price id, missing customer id)
  - Portal session creation fails
  - Return banner shows but subscription doesn’t update
  - Webhook not received / signature invalid
  - Duplicate webhook events / idempotency behavior

- Concrete checks:
  - Where to look in logs (Serilog + Sentry)
  - Stripe dashboard places to check (Events, Customers, Subscriptions)
  - DB tables/entities to inspect: `subscriptions`, `workspaces` billing identity, `idempotency_records`

- Safe remediation steps:
  - How to replay webhooks (high-level)
  - When to run admin plan sync (if applicable)

Avoid vague guidance; make steps actionable.
</action>
<acceptance_criteria>
- `07-OPS-RUNBOOK.md` contains `idempotency_records`
- `07-OPS-RUNBOOK.md` contains `Stripe-Signature`
- `07-OPS-RUNBOOK.md` contains `PaymentsWebhookController`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `rg -n "billing=success|billing=cancel|portal_return" .planning/phases/07-billing-ux-documentation/*.md` shows matches
</verification>

<success_criteria>
- [ ] Billing module overview exists (endpoints + headers + plan codes)
- [ ] Manual UAT checklist exists and covers defined failure paths
- [ ] Ops runbook exists with concrete debugging steps
</success_criteria>

<must_haves>
- D-16 documentation deliverables
- D-17 docs live under `.planning/phases/07-*`
</must_haves>
