---
phase: 08
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - be/src/Syncra.Api/Controllers/UsersController.cs
  - be/src/Syncra.Api/Controllers/WorkspacesController.cs
  - be/src/Syncra.Application/Features/Users/Commands/UpdateUserProfileCommand.cs
  - be/src/Syncra.Application/Features/Workspaces/Commands/UpdateWorkspaceCommand.cs
  - fe/src/pages/app/SettingsPage.tsx
  - fe/src/api/users.ts
  - fe/src/api/workspaces.ts
  - fe/src/hooks/useSettings.ts
autonomous: true
requirements:
  - REQ-8.4 # Integrate Profile & Workspace settings
must_haves:
  truths:
    - User profile and Workspace settings can be updated and are persisted in the backend.
    - Forms use real-time validation with Zod and React Hook Form.
  artifacts:
    - be/src/Syncra.Api/Controllers/UsersController.cs (PUT endpoint)
    - be/src/Syncra.Api/Controllers/WorkspacesController.cs (PUT endpoint)
    - fe/src/hooks/useSettings.ts (TanStack Query hooks for settings)
  key_links:
    - fe/src/pages/app/SettingsPage.tsx -> useUpdateProfile & useUpdateWorkspace hooks.
---

<objective>
Implement the persistence layer for User Profile and Workspace settings.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@be/src/Syncra.Api/Controllers/UsersController.cs
@be/src/Syncra.Api/Controllers/WorkspacesController.cs
@fe/src/pages/app/SettingsPage.tsx
</context>

<tasks>

<task id="5.1" type="auto">
  <name>Implement Backend Profile and Workspace update endpoints</name>
  <files>
    be/src/Syncra.Api/Controllers/UsersController.cs,
    be/src/Syncra.Api/Controllers/WorkspacesController.cs
  </files>
  <action>
    Add update capabilities to the backend:
    - Create update commands and handlers in `Syncra.Application`.
    - Add `PUT /api/v1/users/me` and `PUT /api/v1/workspaces/{id}` to controllers.
  </action>
  <verify>
    <automated>grep "HttpPut" be/src/Syncra.Api/Controllers/UsersController.cs && grep "HttpPut" be/src/Syncra.Api/Controllers/WorkspacesController.cs</automated>
  </verify>
  <done>Backend endpoints for updating profile and workspace are ready.</done>
</task>

<task id="5.2" type="auto">
  <name>Create Frontend API mutations and hooks</name>
  <files>fe/src/api/users.ts, fe/src/api/workspaces.ts, fe/src/hooks/useSettings.ts</files>
  <action>
    Implement frontend mutation logic using TanStack Query `useMutation`:
    - `updateProfile(data)` and `updateWorkspace(id, data)` in `fe/src/api/`.
    - Create `fe/src/hooks/useSettings.ts` to expose these mutations.
  </action>
  <verify>
    <automated>test -f fe/src/api/users.ts && grep "updateProfile" fe/src/api/users.ts && test -f fe/src/hooks/useSettings.ts</automated>
  </verify>
  <done>Frontend API wrappers and hooks for settings are implemented.</done>
</task>

<task id="5.3" type="auto">
  <name>Wire SettingsPage forms with RHF and Zod</name>
  <files>fe/src/pages/app/SettingsPage.tsx</files>
  <action>
    Connect the UI to the backend:
    - Refactor forms in `SettingsPage.tsx` to use `react-hook-form` with `zodResolver`.
    - Handle success/error states in `onSubmit`.
  </action>
  <verify>
    <automated>grep "useForm" fe/src/pages/app/SettingsPage.tsx && grep "zodResolver" fe/src/pages/app/SettingsPage.tsx</automated>
  </verify>
  <done>Settings forms are fully interactive and persist data to the backend.</done>
</task>

</tasks>

<verification>
- \`PUT /api/v1/users/me\` returns 200/204 on valid update.
- Forms display current data on load.
</verification>

<success_criteria>
- [ ] Profile name/email updates are persisted.
- [ ] Workspace name updates are persisted.
</success_criteria>
