---
phase: 08
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - fe/src/api/auth.ts
  - fe/src/api/types.ts
  - fe/vitest.config.ts
  - fe/playwright.config.ts
  - fe/src/context/AuthContext.test.tsx
  - fe/package.json
autonomous: true
requirements:
  - REQ-8.1 # Implement API client (Axios)
  - REQ-8.3 # Connect Authentication & Tenant selection
must_haves:
  truths:
    - Basic API types (User, AuthResponse) are defined.
    - Testing infrastructure (Vitest/Playwright) is ready for use.
    - AuthContext token storage logic is verified by unit tests.
  artifacts:
    - fe/src/api/auth.ts (Auth API client)
    - fe/vitest.config.ts (Unit test config)
    - fe/playwright.config.ts (E2E test config)
    - fe/src/context/AuthContext.test.tsx (Auth unit test)
  key_links:
    - fe/src/context/AuthContext.test.tsx -> Mocks fe/src/lib/axios.ts.
---

<objective>
Establish the API client structure and initialize the testing infrastructure with core verification tests.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@.planning/research/V1.2-FE-RESEARCH.md
</context>

<tasks>

<task id="2.1" type="auto">
  <name>Establish API client structure and Auth client</name>
  <files>fe/src/api/auth.ts, fe/src/api/types.ts</files>
  <action>
    Create the `fe/src/api/` directory:
    - Create `fe/src/api/types.ts` for shared DTO interfaces.
    - Create `fe/src/api/auth.ts` using the Axios `api` instance for login, register, and getMe.
  </action>
  <verify>
    <automated>test -f fe/src/api/auth.ts && grep "getMe" fe/src/api/auth.ts</automated>
  </verify>
  <done>API directory structure started with core Auth endpoints defined.</done>
</task>

<task id="2.2" type="auto">
  <name>Initialize Testing Infrastructure (Vitest & Playwright)</name>
  <files>fe/vitest.config.ts, fe/playwright.config.ts, fe/package.json</files>
  <action>
    Initialize testing configurations:
    - Install devDeps: `vitest`, `@playwright/test`, `@testing-library/react`, `jsdom`.
    - Create `fe/vitest.config.ts` and `fe/playwright.config.ts`.
    - Add test scripts to `fe/package.json`.
  </action>
  <verify>
    <automated>test -f fe/vitest.config.ts && test -f fe/playwright.config.ts && cd fe && npm list vitest @playwright/test @testing-library/react jsdom</automated>
  </verify>
  <done>Testing infrastructure initialized for unit and E2E tests.</done>
</task>

<task id="2.3" type="auto">
  <name>Create AuthContext.test.tsx (Wave 0 Gap)</name>
  <files>fe/src/context/AuthContext.test.tsx</files>
  <action>
    Create a unit test for the AuthContext to verify token storage logic:
    - Mock the Axios API client.
    - Verify login/logout interactions with localStorage.
  </action>
  <verify>
    <automated>npm test:unit fe/src/context/AuthContext.test.tsx</automated>
  </verify>
  <done>AuthContext unit tests created.</done>
</task>

</tasks>

<verification>
- \`npm test:unit fe/src/context/AuthContext.test.tsx\` passes.
- Files exist in \`fe/src/api/\`.
</verification>

<success_criteria>
- [ ] API client follows standard structure.
- [ ] Vitest and Playwright are configured.
- [ ] AuthContext logic is under test.
</success_criteria>
