---
phase: 08
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - fe/src/context/AuthContext.tsx
  - fe/src/context/WorkspaceContext.tsx
  - fe/src/lib/axios.ts
  - fe/src/api/workspaces.ts
autonomous: true
requirements:
  - REQ-8.3 # Connect Authentication & Tenant selection
must_haves:
  truths:
    - Real JWT authentication is active and persists across page reloads.
    - Workspace context is globally available and sent in every API request header.
  artifacts:
    - fe/src/context/WorkspaceContext.tsx (Tenant management)
    - fe/src/api/workspaces.ts (Workspace API client)
  key_links:
    - AuthContext -> fe/src/api/auth.ts (Uses real endpoints)
    - fe/src/lib/axios.ts -> Reads workspace ID from localStorage.
---

<objective>
Refactor the frontend to use real JWT-based authentication and implement the core Workspace (Tenant) context.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@fe/src/context/AuthContext.tsx
@fe/src/lib/axios.ts
</context>

<tasks>

<task id="3.1" type="auto">
  <name>Refactor AuthContext for real API integration</name>
  <files>fe/src/context/AuthContext.tsx</files>
  <action>
    Refactor `AuthContext` to use real API calls and localStorage (per D-06, D-07):
    - State: `user`, `loading`.
    - `login()`: Calls `api/auth.ts`, stores token, fetches `me()`.
    - `useEffect`: Hydrate user on mount if token exists.
  </action>
  <verify>
    <automated>npm test:unit fe/src/context/AuthContext.test.tsx</automated>
  </verify>
  <done>AuthContext handles real login and session hydration from JWT.</done>
</task>

<task id="3.2" type="auto">
  <name>Implement WorkspaceContext and API client</name>
  <files>fe/src/api/workspaces.ts, fe/src/context/WorkspaceContext.tsx</files>
  <action>
    Create workspace management infrastructure (per D-08, D-09):
    - `fe/src/api/workspaces.ts`: Implement `getWorkspaces()`.
    - `fe/src/context/WorkspaceContext.tsx`: Manage `activeWorkspace` using TanStack Query to fetch available workspaces.
  </action>
  <verify>
    <automated>test -f fe/src/context/WorkspaceContext.tsx && grep "activeWorkspace" fe/src/context/WorkspaceContext.tsx</automated>
  </verify>
  <done>WorkspaceContext manages tenant state and persistence using TanStack Query.</done>
</task>

<task id="3.3" type="auto">
  <name>Wire WorkspaceId to Axios Interceptor</name>
  <files>fe/src/lib/axios.ts</files>
  <action>
    Update the Axios Request Interceptor in `fe/src/lib/axios.ts` to use the active workspace ID from localStorage (D-09).
  </action>
  <verify>
    <automated>grep "X-Workspace-Id" fe/src/lib/axios.ts | grep "localStorage.getItem"</automated>
  </verify>
  <done>All outgoing API requests are correctly tagged with the active workspace context.</done>
</task>

</tasks>

<verification>
- \`npm test:unit fe/src/context/AuthContext.test.tsx\` passes.
- Outgoing requests include headers.
</verification>

<success_criteria>
- [ ] User can log in and persist session.
- [ ] Active workspace is sent in every request header.
</success_criteria>
