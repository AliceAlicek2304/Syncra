---
phase: 08
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - fe/src/components/WorkspaceSelector.tsx
  - fe/src/components/Navbar.tsx
  - fe/src/components/ProtectedRoute.tsx
  - fe/src/App.tsx
  - fe/src/components/ProtectedRoute.test.tsx
autonomous: true
requirements:
  - REQ-8.3 # Connect Authentication & Tenant selection
must_haves:
  truths:
    - Protected routes redirect unauthenticated users to the login page.
    - User can switch between workspaces via the UI.
  artifacts:
    - fe/src/components/ProtectedRoute.tsx (Auth guard)
    - fe/src/components/WorkspaceSelector.tsx (UI for switching tenants)
    - fe/src/components/ProtectedRoute.test.tsx (Auth guard unit test)
  key_links:
    - fe/src/App.tsx -> Wraps /app routes in ProtectedRoute.
---

<objective>
Implement the UI components for workspace selection and enforce route protection across the application.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@fe/src/components/Navbar.tsx
</context>

<tasks>

<task id="4.1" type="auto">
  <name>Create ProtectedRoute.test.tsx (TDD)</name>
  <files>fe/src/components/ProtectedRoute.test.tsx</files>
  <action>
    Create a unit test for `ProtectedRoute` BEFORE implementation:
    - Verify it renders children when authenticated.
    - Verify it redirects to `/login` when not authenticated.
  </action>
  <verify>
    <automated>test -f fe/src/components/ProtectedRoute.test.tsx</automated>
  </verify>
  <done>ProtectedRoute unit tests defined.</done>
</task>

<task id="4.2" type="auto">
  <name>Implement ProtectedRoute component</name>
  <files>fe/src/components/ProtectedRoute.tsx, fe/src/App.tsx</files>
  <action>
    Create a `ProtectedRoute` component:
    - Check `isAuthenticated` and `loading` from `AuthContext`.
    - Redirect to `/login` if not authenticated.
    - Wrap `/app/*` routes in `fe/src/App.tsx`.
  </action>
  <verify>
    <automated>npm test:unit fe/src/components/ProtectedRoute.test.tsx</automated>
  </verify>
  <done>Application routes are protected and require a valid JWT session.</done>
</task>

<task id="4.3" type="auto">
  <name>Implement WorkspaceSelector Component</name>
  <files>fe/src/components/WorkspaceSelector.tsx, fe/src/components/Navbar.tsx</files>
  <action>
    Create a `WorkspaceSelector` component (per D-10):
    - Use a dropdown to display available workspaces from `WorkspaceContext`.
    - Integrate the selector into the `Navbar`.
  </action>
  <verify>
    <automated>grep "WorkspaceSelector" fe/src/components/Navbar.tsx</automated>
  </verify>
  <done>User can switch between workspaces via the UI.</done>
</task>

</tasks>

<verification>
- \`npm test:unit fe/src/components/ProtectedRoute.test.tsx\` passes.
- Selector appears in Navbar.
</verification>

<success_criteria>
- [ ] Switching workspace updates the UI context.
- [ ] Protected routes enforce auth.
</success_criteria>
