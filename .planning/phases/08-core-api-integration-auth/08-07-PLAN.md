---
phase: 08-core-api-integration-auth
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [fe/playwright.config.ts, fe/tests/e2e/auth.spec.ts, fe/tests/e2e/workspaces.spec.ts, fe/tests/e2e/ui.spec.ts]
autonomous: true
requirements: [FE-E2E-01, FE-E2E-02, FE-E2E-03, FE-E2E-04, FE-E2E-05, FE-E2E-06, FE-E2E-07]

must_haves:
  truths:
    - "User can run `npm run test:e2e` from the `fe` directory"
    - "Authentication flows (login, hydration, redirect) are verified"
    - "Workspace selection and settings persistence are verified"
    - "Global error notifications and skeleton loaders are verified"
  artifacts:
    - path: "fe/tests/e2e/auth.spec.ts"
      provides: "Auth E2E verification"
    - path: "fe/tests/e2e/workspaces.spec.ts"
      provides: "Workspace & Settings E2E verification"
    - path: "fe/tests/e2e/ui.spec.ts"
      provides: "UI Feedback & Robustness verification"
  key_links:
    - from: "fe/tests/e2e/auth.spec.ts"
      to: "fe/src/context/AuthContext.tsx"
      via: "page.route mocking"
    - from: "fe/tests/e2e/workspaces.spec.ts"
      to: "fe/src/context/WorkspaceContext.tsx"
      via: "localStorage verification"
---

<objective>
Implement a comprehensive Playwright E2E verification suite for Phase 8, covering authentication, workspace management, settings persistence, and UI robustness.

Purpose: Ensure the core integration points between the frontend and (mocked) backend are functioning correctly and that user state is persisted as expected.
Output: Three new Playwright test files in `fe/tests/e2e/`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-core-api-integration-auth/08-UAT.md
@fe/playwright.config.ts
@fe/src/App.tsx
@fe/src/context/AuthContext.tsx
@fe/src/context/WorkspaceContext.tsx
@fe/src/pages/app/SettingsPage.tsx

<interfaces>
From fe/src/api/types.ts:
```typescript
export interface User {
  userId: string;
  email: string;
  displayName?: string;
  firstName?: string;
  lastName?: string;
}

export interface AuthResponse {
  token: string;
  refreshToken: string;
}

export interface Workspace {
  id: string;
  name: string;
  slug: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup E2E Directory & Auth Flow Tests</name>
  <files>fe/playwright.config.ts, fe/tests/e2e/auth.spec.ts</files>
  <action>
    1. Update `fe/playwright.config.ts` to set `baseURL: 'http://localhost:5173/Syncra/'` (matching `vite.config.ts` base).
    2. Create `fe/tests/e2e/` directory.
    3. Create `fe/tests/e2e/auth.spec.ts`.
    4. Implement "Login & Session Hydration" (Req 1):
       - Mock `POST /auth/login` and `GET /auth/me`.
       - Fill login form, submit, verify `syncra_access_token` in localStorage.
       - Reload page, verify user remains logged in (UI shows dashboard).
    5. Implement "Route Protection" (Req 3):
       - Clear localStorage.
       - Attempt to navigate to `/app/dashboard`.
       - Verify redirect to `/Syncra/` (or login modal visibility).
  </action>
  <verify>
    <automated>cd fe && npx playwright test tests/e2e/auth.spec.ts</automated>
  </verify>
  <done>Auth flows and route protection verified by Playwright.</done>
</task>

<task type="auto">
  <name>Task 2: Workspace & Settings Persistence Tests</name>
  <files>fe/tests/e2e/workspaces.spec.ts</files>
  <action>
    1. Create `fe/tests/e2e/workspaces.spec.ts`.
    2. Implement "Workspace Selection & Persistence" (Req 2):
       - Mock `GET /workspaces` returning multiple workspaces.
       - Use WorkspaceSelector to change workspace.
       - Verify `syncra_workspace_id` in localStorage updates.
       - Reload page, verify selected workspace persists in UI.
    3. Implement "Profile Settings Persistence" (Req 4):
       - Mock `PUT /auth/profile` (or relevant endpoint).
       - Navigate to Settings > Profile, update names, submit.
       - Verify success toast appearance.
       - Reload page, verify changes reflect in fields (mock `GET /auth/me` with updated data).
    4. Implement "Workspace Settings Persistence" (Req 5):
       - Mock `PUT /workspaces/:id`.
       - Navigate to Settings > Workspace, update name, submit.
       - Verify success toast and WorkspaceSelector update.
  </action>
  <verify>
    <automated>cd fe && npx playwright test tests/e2e/workspaces.spec.ts</automated>
  </verify>
  <done>Workspace management and settings persistence verified by Playwright.</done>
</task>

<task type="auto">
  <name>Task 3: UI Feedback & State Tests</name>
  <files>fe/tests/e2e/ui.spec.ts</files>
  <action>
    1. Create `fe/tests/e2e/ui.spec.ts`.
    2. Implement "Global Error Notifications" (Req 6):
       - Mock an API call to return 500 or 400 with a message.
       - Trigger the action in UI.
       - Verify toast notification with correct error message appears.
    3. Implement "Glass Skeleton Loaders" (Req 7):
       - Mock an API call (e.g., `GET /workspaces`) with a 2-second delay using `page.route`'s `fulfill`.
       - Navigate to the page.
       - Verify that elements with `.skeleton` class are visible during the delay.
       - Verify they disappear after the mock fulfills.
  </action>
  <verify>
    <automated>cd fe && npx playwright test tests/e2e/ui.spec.ts</automated>
  </verify>
  <done>UI feedback mechanisms verified by Playwright.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Runner → Browser | Playwright executes in a separate process, interacting via CDP |
| Browser → Mocked API | Network requests intercepted by Playwright `page.route` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | Test Scripts | mitigate | Store tests in git, run in clean CI environment |
| T-08-07-02 | Information Disclosure | Mock Data | mitigate | Ensure mock data does not contain real PII or secrets |
</threat_model>

<verification>
Run the full suite from the `fe` directory:
```bash
cd fe
npm run test:e2e
```
</verification>

<success_criteria>
- All 7 requirements from the Phase 8 UAT are covered by automated tests.
- `fe/tests/e2e/` contains 3 passing spec files.
- `npm run test:e2e` executes all tests successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-api-integration-auth/08-07-SUMMARY.md`
</output>
