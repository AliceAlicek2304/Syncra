---
phase: 08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe/package.json
  - fe/src/lib/axios.ts
  - fe/src/App.tsx
autonomous: true
requirements:
  - REQ-8.1 # Implement API client (Axios)
  - REQ-8.2 # Set up React Query
must_haves:
  truths:
    - Axios client and TanStack Query are initialized with global providers.
  artifacts:
    - fe/src/lib/axios.ts (Axios client)
  key_links:
    - fe/src/App.tsx -> Wraps app in QueryClientProvider.
---

<objective>
Establish the foundational API infrastructure for the frontend using Axios and TanStack Query.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@.planning/research/V1.2-FE-RESEARCH.md
@fe/package.json
@fe/src/utils/api.ts
</context>

<tasks>

<task id="1.1" type="auto">
  <name>Install API and state management dependencies</name>
  <files>fe/package.json</files>
  <action>
    Install core libraries for API integration and form handling (per D-01, D-02, D-11, D-12):
    - `axios`, `@tanstack/react-query`, `zod`, `react-hook-form`, `@hookform/resolvers`.
  </action>
  <verify>
    <automated>cd fe && npm list axios @tanstack/react-query zod react-hook-form @hookform/resolvers</automated>
  </verify>
  <done>Dependencies added to package.json and node_modules.</done>
</task>

<task id="1.2" type="auto">
  <name>Create Axios client with interceptors</name>
  <files>fe/src/lib/axios.ts</files>
  <action>
    Create a centralized Axios instance in `fe/src/lib/axios.ts` (per D-01, D-03):
    - Set `baseURL` from `import.meta.env.VITE_API_BASE_URL`.
    - Add Request Interceptor for Bearer token and X-Workspace-Id.
  </action>
  <verify>
    <automated>test -f fe/src/lib/axios.ts && grep "axios.create" fe/src/lib/axios.ts</automated>
  </verify>
  <done>Axios client created with interceptors for JWT and Workspace ID.</done>
</task>

<task id="1.3" type="auto">
  <name>Integrate TanStack Query Provider</name>
  <files>fe/src/App.tsx</files>
  <action>
    Configure React Query for the application (per D-02):
    - Initialize a `QueryClient` and wrap the app with `QueryClientProvider` in `fe/src/App.tsx`.
  </action>
  <verify>
    <automated>grep "QueryClientProvider" fe/src/App.tsx</automated>
  </verify>
  <done>React Query is available globally in the application.</done>
</task>

</tasks>

<verification>
- \`cd fe && npm list\` shows all new dependencies.
- \`fe/src/lib/axios.ts\` exists and correctly exports an axios instance.
</verification>

<success_criteria>
- [ ] Axios instance handles \`VITE_API_BASE_URL\` and header injection.
- [ ] React Query provider is active.
</success_criteria>
