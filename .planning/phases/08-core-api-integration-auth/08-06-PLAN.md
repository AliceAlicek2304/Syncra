---
phase: 08
plan: 06
type: execute
wave: 6
depends_on: ["08-01", "08-05"]
files_modified:
  - fe/src/components/Skeleton.tsx
  - fe/src/lib/axios.ts
  - fe/src/context/ToastContext.tsx
  - fe/src/App.tsx
  - fe/src/pages/app/DashboardPage.tsx
  - fe/src/pages/app/SettingsPage.tsx
  - fe/src/components/Skeleton.module.css
autonomous: true
requirements:
  - REQ-11.2 # Implement Skeleton loaders and Error Boundaries (Partial - Skeletons)
  - REQ-8.5 # Global error/toast handling
must_haves:
  truths:
    - API errors (non-401) trigger a global toast notification.
    - Skeletons are shown during data fetching on Dashboard and Settings pages.
  artifacts:
    - fe/src/context/ToastContext.tsx (Global notifications)
    - fe/src/components/Skeleton.tsx (Reusable glass skeletons)
    - fe/src/components/Skeleton.module.css (Skeleton styles)
  key_links:
    - fe/src/lib/axios.ts -> Interceptor calls useToast on error.
---

<objective>
Enhance the user experience of the API integration by adding global error handling and high-fidelity "Glassmorphism" skeleton loaders.
</objective>

<context>
@.planning/phases/08-CONTEXT.md
@fe/src/components/Toast.tsx
@fe/src/lib/axios.ts
@fe/src/pages/app/DashboardPage.tsx
</context>

<tasks>

<task id="6.1" type="auto">
  <name>Implement Global Toast Context and wire to Axios</name>
  <files>fe/src/context/ToastContext.tsx, fe/src/lib/axios.ts, fe/src/App.tsx</files>
  <action>
    Create a global notification system:
    - Implement `ToastProvider` and wire Axios response interceptor to call `toast.error()` on failure.
  </action>
  <verify>
    <automated>grep "ToastProvider" fe/src/App.tsx && grep "useToast" fe/src/lib/axios.ts</automated>
  </verify>
  <done>API errors automatically trigger visible toast notifications.</done>
</task>

<task id="6.2" type="auto">
  <name>Create Glass Skeleton components</name>
  <files>fe/src/components/Skeleton.tsx, fe/src/components/Skeleton.module.css</files>
  <action>
    Design high-fidelity skeleton loaders with a "glass-shimmer" animation effect.
    Create `fe/src/components/Skeleton.module.css` for styles.
  </action>
  <verify>
    <automated>test -f fe/src/components/Skeleton.tsx && test -f fe/src/components/Skeleton.module.css</automated>
  </verify>
  <done>Reusable skeleton components are available for loading states.</done>
</task>

<task id="6.3" type="auto">
  <name>Integrate skeletons into core app pages</name>
  <files>fe/src/pages/app/DashboardPage.tsx, fe/src/pages/app/SettingsPage.tsx</files>
  <action>
    Apply loading states using TanStack Query `isLoading` to show skeletons on Dashboard and Settings.
  </action>
  <verify>
    <automated>grep "isLoading" fe/src/pages/app/DashboardPage.tsx && grep "Skeleton" fe/src/pages/app/DashboardPage.tsx</automated>
  </verify>
  <done>Core pages display premium skeleton loaders during initial data fetch.</done>
</task>

</tasks>

<verification>
- API error (e.g., 500) triggers a Red Toast.
- Skeletons are visible during loading.
</verification>

<success_criteria>
- [ ] Global error interceptor catches and toasts API errors.
- [ ] Skeletons are visually consistent with the Glassmorphism theme.
</success_criteria>
