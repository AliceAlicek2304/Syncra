---
phase: "02-architectural-performance"
plan: "05"
type: execute
wave: 2
depends_on: ["02-04"]
files_modified: [
  "src/Syncra.Domain/Interfaces/IPlanRepository.cs",
  "src/Syncra.Infrastructure/Repositories/PlanRepository.cs",
  "src/Syncra.Infrastructure/DependencyInjection.cs"
]
autonomous: true
requirements: ["TASK-2.4"]
must_haves:
  truths:
    - "Plans can be looked up by StripePriceId via IPlanRepository"
  artifacts:
    - path: "src/Syncra.Infrastructure/Repositories/PlanRepository.cs"
      provides: "Implementation of IPlanRepository"
  key_links:
    - from: "src/Syncra.Infrastructure/Repositories/PlanRepository.cs"
      to: "src/Syncra.Domain/Entities/Plan.cs"
      via: "EF Core query"
---

<objective>
Implement `IPlanRepository` and its implementation to allow efficient lookups of plans by Stripe identifiers.

Purpose: Provide a clean interface for the application layer to find plans without exposing EF Core details, specifically for subscription-related operations.
Output: `IPlanRepository` interface, `PlanRepository` implementation, and DI registration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architectural-performance/02-04-SUMMARY.md
@src/Syncra.Domain/Entities/Plan.cs
@src/Syncra.Infrastructure/DependencyInjection.cs

<interfaces>
From 02-04 (New Plan entity fields):
```csharp
public string? StripeProductId { get; set; }
public string? StripePriceId { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement IPlanRepository and register it</name>
  <files>src/Syncra.Domain/Interfaces/IPlanRepository.cs, src/Syncra.Infrastructure/Repositories/PlanRepository.cs, src/Syncra.Infrastructure/DependencyInjection.cs</files>
  <action>
    1. Create `src/Syncra.Domain/Interfaces/IPlanRepository.cs` with `Task<Plan?> GetByStripePriceIdAsync(string stripePriceId, CancellationToken cancellationToken = default)`.
    2. Create `src/Syncra.Infrastructure/Repositories/PlanRepository.cs` implementing the interface using `AppDbContext`.
    3. Register the repository in `src/Syncra.Infrastructure/DependencyInjection.cs`.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>IPlanRepository implemented and registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repository -> DB | Queries are performed against the database using Stripe identifiers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | PlanRepository | accept | Returning plan details based on Stripe Price ID is expected behavior. |
</threat_model>

<verification>
1. Verify `PlanRepository` implementation.
2. Verify DI registration in `DependencyInjection.cs`.
</verification>

<success_criteria>
- `IPlanRepository` is available for DI.
- `GetByStripePriceIdAsync` returns the correct Plan entity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architectural-performance/02-05-SUMMARY.md`
</output>
