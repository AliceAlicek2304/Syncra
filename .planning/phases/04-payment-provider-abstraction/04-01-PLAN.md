---
phase: "04-payment-provider-abstraction"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Application/Interfaces/IPaymentProvider.cs",
  "src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs",
  "src/Syncra.Application/DTOs/Payments/*",
  "src/Syncra.Application/Options/PaymentOptions.cs",
  "src/Syncra.Domain/Entities/Workspace.cs",
  "src/Syncra.Domain/Entities/Subscription.cs",
  "src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs",
  "src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs",
  "src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs",
  "src/Syncra.Infrastructure/Repositories/SubscriptionRepository.cs",
  "src/Syncra.Infrastructure/Services/StripePaymentProvider.cs",
  "src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs",
  "src/Syncra.Infrastructure/DependencyInjection.cs",
  "src/Syncra.Infrastructure/Migrations/*",
  "tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs"
]
autonomous: true
requirements: ["1.1"]
must_haves:
  truths:
    - "Application-facing billing contracts are provider-neutral and do not expose Stripe-specific service names"
    - "Workspace stores canonical billing identity via BillingProvider and BillingCustomerId while preserving StripeCustomerId for transition compatibility"
    - "Subscription canonical identity is Provider plus ProviderSubscriptionId, and repository APIs use provider-neutral names"
    - "DI can resolve a provider by stable lowercase identifier and defaults to stripe when no explicit provider exists yet"
  artifacts:
    - path: "src/Syncra.Application/Interfaces/IPaymentProvider.cs"
      provides: "Provider-neutral billing contract for checkout, portal, and webhook parsing"
    - path: "src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs"
      provides: "Registry/resolver for provider selection by identifier"
    - path: "src/Syncra.Domain/Entities/Workspace.cs"
      provides: "Canonical workspace billing identity fields"
    - path: "src/Syncra.Infrastructure/Migrations/*"
      provides: "Database backfill for generic billing identity"
---

<objective>
Lay the foundation for payment-provider abstraction before any API flow changes.

Purpose: establish provider-neutral contracts, canonical billing identity fields, and DI-based provider resolution so later plans can switch checkout, portal, and webhooks without breaking existing Stripe behavior.
Output: `IPaymentProvider`, resolver/registry wiring, generalized billing persistence, and a migration that backfills Stripe data into the new canonical fields.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/get-shit-done/workflows/execute-plan.md
@$HOME/.codeium/windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-CONTEXT.md
@.planning/phases/04-RESEARCH.md
@src/Syncra.Application/Interfaces/IStripeService.cs
@src/Syncra.Infrastructure/Services/StripeService.cs
@src/Syncra.Domain/Entities/Workspace.cs
@src/Syncra.Domain/Entities/Subscription.cs
@src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs
@src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs
@src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs
@src/Syncra.Infrastructure/Repositories/SubscriptionRepository.cs
@src/Syncra.Infrastructure/DependencyInjection.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce provider-neutral billing contracts and normalized DTOs</name>
  <files>src/Syncra.Application/Interfaces/IPaymentProvider.cs, src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs, src/Syncra.Application/DTOs/Payments/*, src/Syncra.Application/Options/PaymentOptions.cs</files>
  <read_first>
    - .planning/phases/04-CONTEXT.md
    - .planning/phases/04-RESEARCH.md
    - src/Syncra.Application/Interfaces/IStripeService.cs
    - src/Syncra.Infrastructure/Services/StripeService.cs
  </read_first>
  <action>
    1. Create `src/Syncra.Application/Interfaces/IPaymentProvider.cs` with a provider-neutral contract:
       - `string ProviderKey { get; }`
       - `Task<PaymentCheckoutSessionResult> CreateCheckoutSessionAsync(PaymentCheckoutSessionRequest request, CancellationToken cancellationToken = default)`
       - `Task<PaymentPortalSessionResult> CreatePortalSessionAsync(PaymentPortalSessionRequest request, CancellationToken cancellationToken = default)`
       - `Task<PaymentWebhookParseResult> ParseWebhookAsync(PaymentWebhookRequest request, CancellationToken cancellationToken = default)`
    2. Create `src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs` with exact methods:
       - `IPaymentProvider GetRequiredProvider(string providerKey)`
       - `string GetDefaultProviderKey()`
    3. Add provider-neutral DTOs under `src/Syncra.Application/DTOs/Payments/` with concrete names used by later plans:
       - `PaymentCheckoutSessionRequest`
       - `PaymentCheckoutSessionResult`
       - `PaymentPortalSessionRequest`
       - `PaymentPortalSessionResult`
       - `PaymentWebhookRequest`
       - `PaymentWebhookEvent`
       - `PaymentWebhookParseResult`
    4. Normalize webhook payload modeling so later webhook code can work without Stripe SDK types outside Infrastructure. `PaymentWebhookEvent` must include:
       - `Provider`
       - `EventId`
       - `EventType`
       - `WorkspaceId` (nullable)
       - `ProviderCustomerId` (nullable)
       - `ProviderSubscriptionId` (nullable)
       - `Metadata` dictionary
    5. Create `src/Syncra.Application/Options/PaymentOptions.cs` with `SectionName = "Payments"` and `DefaultProvider = "stripe"` so fallback resolution is explicit and stable.
  </action>
  <acceptance_criteria>
    - `src/Syncra.Application/Interfaces/IPaymentProvider.cs` contains `interface IPaymentProvider`
    - `src/Syncra.Application/Interfaces/IPaymentProvider.cs` contains `string ProviderKey { get; }`
    - `src/Syncra.Application/Interfaces/IPaymentProvider.cs` contains `CreateCheckoutSessionAsync`
    - `src/Syncra.Application/Interfaces/IPaymentProvider.cs` contains `CreatePortalSessionAsync`
    - `src/Syncra.Application/Interfaces/IPaymentProvider.cs` contains `ParseWebhookAsync`
    - `src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs` contains `GetRequiredProvider(string providerKey)`
    - `src/Syncra.Application/Options/PaymentOptions.cs` contains `DefaultProvider = "stripe"`
    - `src/Syncra.Application/DTOs/Payments/` contains files defining `PaymentWebhookEvent` and `PaymentCheckoutSessionResult`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Provider-neutral contracts compile and define the exact request/result shapes needed by later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize billing identity in domain, persistence, and repositories</name>
  <files>src/Syncra.Domain/Entities/Workspace.cs, src/Syncra.Domain/Entities/Subscription.cs, src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs, src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs, src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs, src/Syncra.Infrastructure/Repositories/SubscriptionRepository.cs, src/Syncra.Infrastructure/Migrations/*</files>
  <read_first>
    - .planning/phases/04-CONTEXT.md
    - src/Syncra.Domain/Entities/Workspace.cs
    - src/Syncra.Domain/Entities/Subscription.cs
    - src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs
    - src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs
    - src/Syncra.Infrastructure/Persistence/Configurations/SubscriptionConfigurations.cs
    - src/Syncra.Infrastructure/Repositories/SubscriptionRepository.cs
  </read_first>
  <action>
    1. Update `Workspace` to add canonical fields:
       - `public string? BillingProvider { get; private set; }`
       - `public string? BillingCustomerId { get; private set; }`
    2. Add a single domain method on `Workspace` with this exact responsibility: set canonical billing identity and keep `StripeCustomerId` synchronized when `provider == "stripe"`.
    3. Update `WorkspaceConfiguration` to map:
       - `BillingProvider` -> `billing_provider` with max length 50
       - `BillingCustomerId` -> `billing_customer_id` with max length 200
    4. Remove reliance on `Subscription.StripeSubscriptionId` as canonical identity. Either delete the property or mark it unused, but the repository and handlers must move to `Provider` + `ProviderSubscriptionId` only.
    5. Rename repository contract and implementation methods to provider-neutral names:
       - `GetByWorkspaceIdAsync(Guid workspaceId)` returns `Task<Subscription?>`
       - `GetByProviderSubscriptionIdAsync(string provider, string providerSubscriptionId)`
    6. Update `SubscriptionRepository` to query both provider and `ProviderSubscriptionId` for provider-aware lookups.
    7. Create an EF migration that:
       - adds `billing_provider` and `billing_customer_id` to `workspaces`
       - backfills `billing_provider = 'stripe'` and `billing_customer_id = stripe_customer_id` where `stripe_customer_id` exists
       - backfills `provider_subscription_id` from existing Stripe subscription data where needed
       - drops the old Stripe-specific subscription column if it still exists in the current model
  </action>
  <acceptance_criteria>
    - `src/Syncra.Domain/Entities/Workspace.cs` contains `BillingProvider`
    - `src/Syncra.Domain/Entities/Workspace.cs` contains `BillingCustomerId`
    - `src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs` contains `billing_provider`
    - `src/Syncra.Infrastructure/Persistence/Configurations/WorkspaceConfigurations.cs` contains `billing_customer_id`
    - `src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs` contains `GetByProviderSubscriptionIdAsync`
    - `src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs` contains `Task<Subscription?> GetByWorkspaceIdAsync`
    - `src/Syncra.Infrastructure/Repositories/SubscriptionRepository.cs` contains `s.Provider == provider`
    - `src/Syncra.Infrastructure/Migrations/` contains a migration that references `billing_provider` and `billing_customer_id`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Domain</automated>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>Canonical billing identity is stored generically and repository APIs no longer expose Stripe-specific names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapt Stripe infrastructure into a resolvable payment provider</name>
  <files>src/Syncra.Infrastructure/Services/StripePaymentProvider.cs, src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs, src/Syncra.Infrastructure/DependencyInjection.cs</files>
  <read_first>
    - src/Syncra.Infrastructure/Services/StripeService.cs
    - src/Syncra.Infrastructure/DependencyInjection.cs
    - src/Syncra.Application/Interfaces/IPaymentProvider.cs
    - src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs
    - src/Syncra.Application/Options/PaymentOptions.cs
  </read_first>
  <action>
    1. Create `StripePaymentProvider` as the Stripe implementation of `IPaymentProvider`.
    2. Move the existing checkout, portal, customer lookup/create, and webhook verification/parsing responsibilities from `StripeService` into `StripePaymentProvider` without changing Stripe API behavior.
    3. Ensure `StripePaymentProvider.ProviderKey` returns the exact string `"stripe"`.
    4. In webhook parsing, convert Stripe SDK event objects into `PaymentWebhookEvent` values so no controller/application handler needs `Stripe.Event`, `Stripe.Checkout.Session`, or `Stripe.Subscription`.
    5. Create `PaymentProviderResolver` that accepts all registered `IPaymentProvider` instances plus `IOptions<PaymentOptions>` and:
       - stores providers by lowercase `ProviderKey`
       - returns the configured default provider key
       - throws a clear exception for unknown providers
    6. Update `DependencyInjection` to:
       - bind `PaymentOptions`
       - register `StripePaymentProvider` as `IPaymentProvider`
       - register `IPaymentProviderResolver`
       - stop exposing `IStripeService` as the main application dependency for new code paths
  </action>
  <acceptance_criteria>
    - `src/Syncra.Infrastructure/Services/StripePaymentProvider.cs` contains `class StripePaymentProvider`
    - `src/Syncra.Infrastructure/Services/StripePaymentProvider.cs` contains `ProviderKey => "stripe"`
    - `src/Syncra.Infrastructure/Services/StripePaymentProvider.cs` contains `ParseWebhookAsync`
    - `src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs` contains `class PaymentProviderResolver`
    - `src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs` contains `providers.ToDictionary`
    - `src/Syncra.Infrastructure/DependencyInjection.cs` contains `Configure<PaymentOptions>`
    - `src/Syncra.Infrastructure/DependencyInjection.cs` contains `AddScoped<IPaymentProvider, StripePaymentProvider>`
    - `src/Syncra.Infrastructure/DependencyInjection.cs` contains `AddScoped<IPaymentProviderResolver, PaymentProviderResolver>`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>Stripe is now available through a provider registry instead of a Stripe-specific application service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add focused resolver tests for provider registration and default fallback</name>
  <files>tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs</files>
  <read_first>
    - src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs
    - src/Syncra.Infrastructure/Services/PaymentProviderResolver.cs
    - tests/Syncra.UnitTests/Infrastructure/StripeServiceTests.cs
  </read_first>
  <action>
    Create `PaymentProviderResolverTests.cs` with at least these test cases:
    1. Resolving `"stripe"` returns the registered Stripe provider.
    2. Provider keys are matched case-insensitively by normalizing to lowercase.
    3. `GetDefaultProviderKey()` returns `"stripe"` when no override is provided.
    4. Unknown provider keys throw a deterministic exception that includes the missing provider name.
  </action>
  <acceptance_criteria>
    - `tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs` contains `Resolving_stripe_returns_registered_provider`
    - `tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs` contains `GetDefaultProviderKey`
    - `tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs` contains `unknown provider`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~PaymentProviderResolverTests</automated>
  </verify>
  <done>Provider registry behavior is pinned down by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application -> Infrastructure provider registry | Only registered provider identifiers should be resolvable. |
| Existing billing data -> migration backfill | Stripe-specific historical data is being generalized and must not be corrupted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Provider resolution | mitigate | Resolver normalizes keys and rejects unknown providers instead of silently falling back. |
| T-04-01-02 | Data Integrity | Billing migration | mitigate | Migration backfills from existing Stripe columns into canonical fields before removing old subscription identity state. |
| T-04-01-03 | Information Disclosure | Payment DTO boundary | mitigate | Controllers and handlers consume normalized DTOs rather than raw Stripe SDK objects. |
</threat_model>

<verification>
- Run `dotnet build src/Syncra.Application`.
- Run `dotnet build src/Syncra.Infrastructure`.
- Run `dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~PaymentProviderResolverTests`.
</verification>

<success_criteria>
- Provider-neutral contracts exist and are usable by later plans.
- Canonical workspace/subscription billing identity is generalized without breaking Stripe fallback.
- Stripe is registerable and resolvable as the first provider.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-provider-abstraction/04-01-SUMMARY.md`
</output>
