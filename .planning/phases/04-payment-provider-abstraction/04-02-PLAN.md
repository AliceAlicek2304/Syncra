---
phase: "04-payment-provider-abstraction"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [
  "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommand.cs",
  "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs",
  "src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs",
  "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommand.cs",
  "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs",
  "src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs",
  "src/Syncra.Domain/Interfaces/IPlanRepository.cs",
  "src/Syncra.Infrastructure/Repositories/PlanRepository.cs",
  "src/Syncra.Api/Controllers/SubscriptionsController.cs",
  "src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs",
  "tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionCommandHandlerTests.cs",
  "tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionByPlanCommandHandlerTests.cs",
  "tests/Syncra.UnitTests/Application/Subscriptions/CreatePortalSessionCommandHandlerTests.cs"
]
autonomous: true
requirements: ["1.1"]
must_haves:
  truths:
    - "Existing v1 checkout remains backward-compatible and still accepts provider price identifiers through `priceId`"
    - "A new v2 checkout flow accepts `planCode` and resolves the current provider-specific price identifier from persisted plan data"
    - "Checkout and portal handlers resolve the provider from existing subscription or workspace billing identity, otherwise use the configured default provider"
    - "Application handlers call `IPaymentProviderResolver` instead of a Stripe-specific application service"
  artifacts:
    - path: "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs"
      provides: "Backward-compatible v1 checkout flow through provider abstraction"
    - path: "src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs"
      provides: "New v2 planCode checkout orchestration"
    - path: "src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs"
      provides: "Versioned v2 checkout endpoint using planCode"
---

<objective>
Refactor checkout and portal flows to use the new payment-provider abstraction while preserving the existing v1 checkout contract.

Purpose: decouple application billing flows from `IStripeService`, add a forward-compatible `planCode` checkout API, and keep current clients working without changes.
Output: provider-aware checkout/portal handlers, a new v2 checkout endpoint, and focused tests for provider resolution + plan-based checkout.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/get-shit-done/workflows/execute-plan.md
@$HOME/.codeium/windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-CONTEXT.md
@.planning/phases/04-RESEARCH.md
@src/Syncra.Application/Interfaces/IPaymentProvider.cs
@src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs
@src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommand.cs
@src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs
@src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs
@src/Syncra.Api/Controllers/SubscriptionsController.cs
@src/Syncra.Domain/Interfaces/IPlanRepository.cs
@src/Syncra.Infrastructure/Repositories/PlanRepository.cs
@src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs
@src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor existing checkout and portal handlers to resolve providers dynamically</name>
  <files>src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs, src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs, src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommand.cs</files>
  <read_first>
    - .planning/phases/04-CONTEXT.md
    - src/Syncra.Application/Interfaces/IPaymentProviderResolver.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommand.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs
    - src/Syncra.Domain/Interfaces/ISubscriptionRepository.cs
    - src/Syncra.Domain/Entities/Workspace.cs
  </read_first>
  <action>
    1. Replace `IStripeService` injection in both handlers with `IPaymentProviderResolver`.
    2. Inject `ISubscriptionRepository` into both handlers so provider selection can follow the Phase 4 decision order:
       - existing subscription provider if a subscription row exists and `Provider` is not null/empty
       - otherwise `workspace.BillingProvider` if present
       - otherwise `resolver.GetDefaultProviderKey()`
    3. Keep `CreateCheckoutSessionCommand` as the v1 command and keep the request property named `PriceId` to preserve the current API contract.
    4. In `CreateCheckoutSessionCommandHandler`, build a `PaymentCheckoutSessionRequest` with exact fields sourced from the current models:
       - `WorkspaceId = workspace.Id`
       - `WorkspaceName = workspace.Name.ToString()`
       - `ProviderCustomerId = workspace.BillingCustomerId ?? workspace.StripeCustomerId`
       - `PriceId = request.PriceId`
       - `SuccessUrl = request.SuccessUrl ?? string.Empty`
       - `CancelUrl = request.CancelUrl ?? string.Empty`
    5. In `CreatePortalSessionCommandHandler`, build a `PaymentPortalSessionRequest` with:
       - `WorkspaceId = workspace.Id`
       - `WorkspaceName = workspace.Name.ToString()`
       - `ProviderCustomerId = workspace.BillingCustomerId ?? workspace.StripeCustomerId`
       - `ReturnUrl = request.ReturnUrl ?? string.Empty`
    6. Map provider results back into the existing response DTOs so the API response shape remains unchanged.
  </action>
  <acceptance_criteria>
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs` contains `IPaymentProviderResolver`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs` contains `IPaymentProviderResolver`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs` contains `GetDefaultProviderKey()`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs` contains `workspace.BillingCustomerId ?? workspace.StripeCustomerId`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs` contains `workspace.BillingCustomerId ?? workspace.StripeCustomerId`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommand.cs` still contains `string PriceId`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Existing checkout and portal handlers are provider-aware without breaking the v1 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add planCode-based checkout command and plan lookup by code</name>
  <files>src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommand.cs, src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs, src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs, src/Syncra.Domain/Interfaces/IPlanRepository.cs, src/Syncra.Infrastructure/Repositories/PlanRepository.cs</files>
  <read_first>
    - .planning/phases/04-CONTEXT.md
    - src/Syncra.Domain/Entities/Plan.cs
    - src/Syncra.Domain/Interfaces/IPlanRepository.cs
    - src/Syncra.Infrastructure/Repositories/PlanRepository.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs
    - src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs
  </read_first>
  <action>
    1. Add `CreateCheckoutSessionByPlanCommand(Guid WorkspaceId, string PlanCode, string? SuccessUrl, string? CancelUrl) : IRequest<CreateCheckoutSessionResponse>`.
    2. Extend `CheckoutSessionDto.cs` with a new request DTO named `CreateCheckoutSessionByPlanRequest(string PlanCode, string? SuccessUrl, string? CancelUrl)`.
    3. Extend `IPlanRepository` and `PlanRepository` with `Task<Plan?> GetByCodeAsync(string code, CancellationToken cancellationToken = default)`.
    4. Create `CreateCheckoutSessionByPlanCommandHandler` that:
       - loads the workspace
       - resolves the current provider using the same precedence as Task 1
       - loads the requested plan by `PlanCode`
       - validates the plan exists and `plan.IsActive == true`
       - for `provider == "stripe"`, uses `plan.StripePriceId` as the provider price identifier
       - throws a deterministic domain/application error if the plan lacks the needed provider price identifier
       - calls the resolved provider and returns `CreateCheckoutSessionResponse`
    5. Keep Phase 4 scoped to abstraction only: do not implement broader plan reconciliation here beyond using persisted `Plan` data.
  </action>
  <acceptance_criteria>
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommand.cs` contains `string PlanCode`
    - `src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs` contains `CreateCheckoutSessionByPlanRequest`
    - `src/Syncra.Domain/Interfaces/IPlanRepository.cs` contains `GetByCodeAsync`
    - `src/Syncra.Infrastructure/Repositories/PlanRepository.cs` contains `p.Code == code`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs` contains `plan.StripePriceId`
    - `src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs` contains `plan.IsActive`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>The application can create checkout sessions from a stable plan code rather than a raw provider price id.</done>
</task>

<task type="auto">
  <name>Task 3: Add a versioned v2 checkout API while preserving the v1 routes</name>
  <files>src/Syncra.Api/Controllers/SubscriptionsController.cs, src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs, src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs</files>
  <read_first>
    - .planning/phases/04-CONTEXT.md
    - src/Syncra.Api/Controllers/SubscriptionsController.cs
    - src/Syncra.Application/DTOs/Subscriptions/CheckoutSessionDto.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommand.cs
  </read_first>
  <action>
    1. Keep `SubscriptionsController` route exactly `api/v1/workspaces/{workspaceId}/subscription`.
    2. Keep the v1 `create-checkout-session` action unchanged at the API surface:
       - request type remains `CreateCheckoutSessionRequest`
       - request field remains `PriceId`
       - response remains `CreateCheckoutSessionResponse`
    3. Create `SubscriptionsV2Controller.cs` with route `api/v2/workspaces/{workspaceId}/subscription`.
    4. Add `POST create-checkout-session` to the v2 controller that accepts `CreateCheckoutSessionByPlanRequest` and sends `CreateCheckoutSessionByPlanCommand`.
    5. Do not add a separate v2 portal endpoint in this phase; keep portal creation on the existing v1 route while its handler becomes provider-neutral internally.
  </action>
  <acceptance_criteria>
    - `src/Syncra.Api/Controllers/SubscriptionsController.cs` contains `[Route("api/v1/workspaces/{workspaceId}/subscription")]`
    - `src/Syncra.Api/Controllers/SubscriptionsController.cs` still contains `CreateCheckoutSessionRequest request`
    - `src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs` contains `[Route("api/v2/workspaces/{workspaceId}/subscription")]`
    - `src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs` contains `CreateCheckoutSessionByPlanRequest request`
    - `src/Syncra.Api/Controllers/SubscriptionsV2Controller.cs` contains `new CreateCheckoutSessionByPlanCommand`
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Syncra.Api</automated>
  </verify>
  <done>The API exposes a forward-compatible v2 checkout route without breaking existing clients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add focused handler tests for provider selection and planCode checkout</name>
  <files>tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionCommandHandlerTests.cs, tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionByPlanCommandHandlerTests.cs, tests/Syncra.UnitTests/Application/Subscriptions/CreatePortalSessionCommandHandlerTests.cs</files>
  <read_first>
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionCommandHandler.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreateCheckoutSessionByPlanCommandHandler.cs
    - src/Syncra.Application/Features/Subscriptions/Commands/CreatePortalSessionCommandHandler.cs
    - tests/Syncra.UnitTests/Infrastructure/PaymentProviderResolverTests.cs
  </read_first>
  <action>
    Add handler tests covering at least these cases:
    1. Existing subscription with `Provider = "stripe"` causes checkout and portal flows to resolve `stripe` even when no workspace billing provider is set.
    2. Missing subscription falls back to `workspace.BillingProvider` when present.
    3. Missing subscription and missing workspace billing provider falls back to `GetDefaultProviderKey()`.
    4. `CreateCheckoutSessionByPlanCommandHandler` resolves a plan by `PlanCode` and uses `plan.StripePriceId`.
    5. `CreateCheckoutSessionByPlanCommandHandler` fails deterministically when the plan is missing or inactive.
  </action>
  <acceptance_criteria>
    - `tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionCommandHandlerTests.cs` contains `workspace.BillingProvider`
    - `tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionCommandHandlerTests.cs` contains `GetDefaultProviderKey`
    - `tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionByPlanCommandHandlerTests.cs` contains `PlanCode`
    - `tests/Syncra.UnitTests/Application/Subscriptions/CreateCheckoutSessionByPlanCommandHandlerTests.cs` contains `StripePriceId`
    - `tests/Syncra.UnitTests/Application/Subscriptions/CreatePortalSessionCommandHandlerTests.cs` contains `Provider = "stripe"`
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~CreateCheckoutSessionCommandHandlerTests</automated>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~CreateCheckoutSessionByPlanCommandHandlerTests</automated>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~CreatePortalSessionCommandHandlerTests</automated>
  </verify>
  <done>Checkout and portal provider-selection rules are pinned down by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API client -> checkout endpoints | v1 and v2 routes accept different identifiers and must validate them deterministically. |
| Application -> provider abstraction | Handler logic must resolve the correct provider and avoid silently using the wrong billing identity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Checkout routing | mitigate | v1 preserves raw `PriceId` behavior while v2 validates `PlanCode` against persisted plans before calling a provider. |
| T-04-02-02 | Spoofing | Provider selection | mitigate | Handler precedence is explicit: subscription provider, then workspace billing provider, then configured default. |
| T-04-02-03 | Denial of Service | Invalid plan requests | mitigate | Inactive or missing plans fail fast before external provider calls are attempted. |
</threat_model>

<verification>
- Run `dotnet build src/Syncra.Application`.
- Run `dotnet build src/Syncra.Api`.
- Run the focused checkout/portal handler tests.
</verification>

<success_criteria>
- Existing v1 clients keep working with `priceId`.
- New v2 checkout supports `planCode`.
- Application handlers no longer depend on `IStripeService` directly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-provider-abstraction/04-02-SUMMARY.md`
</output>
