---
phase: "03-quality-observability"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/Syncra.Domain/Enums/IntegrationRefreshHealthStatus.cs",
  "src/Syncra.Domain/Entities/Integration.cs",
  "src/Syncra.Infrastructure/Persistence/Configurations/IntegrationConfiguration.cs",
  "src/Syncra.Infrastructure/Migrations/*",
  "src/Syncra.Application/Services/IntegrationTokenRefreshService.cs",
  "src/Syncra.Application/Features/Integrations/Queries/GetIntegrationHealthQueryHandler.cs",
  "src/Syncra.Application/Features/Integrations/IntegrationMapper.cs",
  "src/Syncra.Application/DTOs/Integrations/IntegrationDto.cs",
  "src/Syncra.Application/Services/IntegrationAnalyticsService.cs",
  "src/Syncra.Application/Services/PublishService.cs",
  "tests/Syncra.UnitTests/Application/IntegrationTokenRefreshServiceTests.cs",
  "tests/Syncra.UnitTests/Application/GetIntegrationHealthQueryHandlerTests.cs"
]
autonomous: true
requirements: ["3.1"]
must_haves:
  truths:
    - "Integrations track token refresh health with ok/warning/error/needs_reauth states and a persisted consecutive-failure counter"
    - "Terminal refresh errors immediately mark integration as needs_reauth"
    - "Non-terminal refresh failures follow ladder: 1=warning, 2=error, 3=needs_reauth"
    - "Any successful refresh resets consecutive failures to 0 and clears needs_reauth"
    - "Integration health endpoint returns status with precedence: disconnected > token_expired > needs_reauth > error > warning > ok"
    - "Publishing and analytics are blocked when an integration is in needs_reauth"
  artifacts:
    - path: "src/Syncra.Domain/Entities/Integration.cs"
      provides: "Persisted token refresh health state + consecutive failures"
    - path: "src/Syncra.Application/Services/IntegrationTokenRefreshService.cs"
      provides: "Refresh attempt auditing + failure ladder logic"
    - path: "src/Syncra.Application/Features/Integrations/Queries/GetIntegrationHealthQueryHandler.cs"
      provides: "Public health status computation"
---

<objective>
Implement OAuth token refresh health tracking and auditing so integration stability is transparent and actionable.

Purpose: detect broken integrations early, surface a stable health status for UI/ops, and prevent downstream failures.
Output: persisted failure ladder (`warning`/`error`/`needs_reauth`), richer refresh logs, and API health status precedence.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-CONTEXT.md
@src/Syncra.Domain/Entities/Integration.cs
@src/Syncra.Domain/Enums/IntegrationRefreshHealthStatus.cs
@src/Syncra.Application/Services/IntegrationTokenRefreshService.cs
@src/Syncra.Application/Features/Integrations/Queries/GetIntegrationHealthQueryHandler.cs
@src/Syncra.Application/Services/IntegrationAnalyticsService.cs
@src/Syncra.Application/Services/PublishService.cs
@src/Syncra.Infrastructure/Persistence/Interceptors/AuditInterceptor.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Integration health model (enum + consecutive failures)</name>
  <files>src/Syncra.Domain/Enums/IntegrationRefreshHealthStatus.cs, src/Syncra.Domain/Entities/Integration.cs, src/Syncra.Infrastructure/Persistence/Configurations/IntegrationConfiguration.cs</files>
  <action>
    1. Extend `IntegrationRefreshHealthStatus` to include `NeedsReauth`.
    2. Add a persisted integer counter on `Integration`: `TokenRefreshConsecutiveFailures`.
    3. Update `Integration` domain behaviors:
       - Successful refresh sets status to `Ok`, clears last error, and resets consecutive failures to 0.
       - Failed refresh increments consecutive failures and sets status based on ladder:
         - 1 => `Warning`
         - 2 => `Error`
         - 3+ => `NeedsReauth`
       - Terminal errors (see Phase 3 CONTEXT.md) immediately set status `NeedsReauth`.
    4. Update EF mapping in `IntegrationConfiguration`:
       - Map `TokenRefreshConsecutiveFailures` to `token_refresh_consecutive_failures` with default 0.
       - Keep `TokenRefreshHealthStatus` stored as string.

    Note: Keep `TokenRefreshLastAttemptAtUtc`, `TokenRefreshLastSuccessAtUtc`, and `TokenRefreshLastError` semantics intact, but ensure they’re updated on every attempt.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Domain</automated>
    <automated>dotnet build src/Syncra.Infrastructure</automated>
  </verify>
  <done>Domain compiles and IntegrationConfiguration maps new property.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration for new Integration fields</name>
  <files>src/Syncra.Infrastructure/Migrations/</files>
  <action>
    1. Run `dotnet ef migrations add AddIntegrationTokenRefreshFailureLadder --project src/Syncra.Infrastructure --startup-project src/Syncra.Api`.
    2. (Optional) Apply locally: `dotnet ef database update --project src/Syncra.Infrastructure --startup-project src/Syncra.Api`.
  </action>
  <verify>
    <automated>ls src/Syncra.Infrastructure/Migrations/*AddIntegrationTokenRefreshFailureLadder*.cs</automated>
  </verify>
  <done>Migration exists and includes new column + enum storage updates as needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update IntegrationTokenRefreshService refresh attempt behavior</name>
  <files>src/Syncra.Application/Services/IntegrationTokenRefreshService.cs</files>
  <action>
    Update `RefreshExpiringIntegrationsAsync` to match Phase 3 decisions:

    Eligibility rules:
    - Attempt refresh when token is expiring/expired OR health status is `Warning`/`Error`.
    - Do not attempt refresh when integration is inactive, missing refresh token, or health is `NeedsReauth`.

    Auditing/logging rules:
    - Call `integration.MarkTokenRefreshAttempt(utcNow)` immediately before calling provider refresh.
    - Log a single structured line per attempt with:
      integrationId, workspaceId, platform, attemptAt, result(success/failure), statusAfter, consecutiveFailuresAfter.

    Failure classification:
    - Detect terminal errors when provider error code/details indicate: invalid_grant / invalid_token / token_revoked,
      and mark integration `NeedsReauth` immediately.
    - Otherwise apply the failure ladder.

    Persist updates through repository + unit of work as currently implemented.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Refresh service behavior matches eligibility and ladder rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update API-facing integration health status computation</name>
  <files>src/Syncra.Application/Features/Integrations/Queries/GetIntegrationHealthQueryHandler.cs</files>
  <action>
    Update `GetIntegrationHealthQueryHandler` to emit statuses per Phase 3 precedence:
    - disconnected
    - token_expired
    - needs_reauth
    - error
    - warning
    - ok

    Use `integration.IsActive`, `integration.IsTokenExpired`, and `integration.TokenRefreshHealthStatus` (including NeedsReauth) to compute the final `Status` string.
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Health query handler compiles and maps all states.</done>
</task>

<task type="auto">
  <name>Task 5: Gate analytics + publishing when integration is needs_reauth</name>
  <files>src/Syncra.Application/Services/IntegrationAnalyticsService.cs, src/Syncra.Application/Services/PublishService.cs</files>
  <action>
    Enforce Phase 3 rule: integrations in `needs_reauth` must not be used.

    - In `IntegrationAnalyticsService`, return a failure `Result<T>` with a stable error message when the integration is in NeedsReauth.
    - In `PublishService`, prevent publishing and return an error (exception or failure result depending on existing patterns) when integration is in NeedsReauth.

    Keep behavior unchanged for disconnected integrations (existing checks remain authoritative).
  </action>
  <verify>
    <automated>dotnet build src/Syncra.Application</automated>
  </verify>
  <done>Analytics and publish paths are blocked under needs_reauth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Add/enable unit tests for refresh ladder + health mapping</name>
  <files>tests/Syncra.UnitTests/Application/IntegrationTokenRefreshServiceTests.cs, tests/Syncra.UnitTests/Application/GetIntegrationHealthQueryHandlerTests.cs</files>
  <action>
    1. Enable `IntegrationTokenRefreshServiceTests.cs` by removing the `#if FALSE` guard.
    2. Add tests to cover:
       - 1st consecutive failure => Warning
       - 2nd consecutive failure => Error
       - 3rd consecutive failure => NeedsReauth
       - Terminal error (invalid_grant) => NeedsReauth immediately
       - Success resets consecutive failures to 0 and status to Ok
    3. Add `GetIntegrationHealthQueryHandlerTests` to validate precedence ordering.
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~IntegrationTokenRefreshServiceTests</automated>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~GetIntegrationHealthQueryHandlerTests</automated>
  </verify>
  <done>Unit tests pass for ladder + health mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Social provider -> Token refresh job | OAuth token endpoints are external and untrusted. |
| Application -> Domain | Domain health state affects whether critical operations (publish/analytics) can proceed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | Refresh job | mitigate | NeedsReauth state prevents repeated failing refresh attempts; structured logs support diagnosis. |
| T-03-01-02 | Information Disclosure | Token refresh errors | mitigate | Truncate stored errors (`TokenRefreshLastErrorMaxLength`) and avoid logging raw secrets. |
</threat_model>

<verification>
1. Run focused unit tests for token refresh ladder + health mapping.
2. Run `dotnet test tests/Syncra.UnitTests` once at end of wave to ensure no regressions.
</verification>

<success_criteria>
- Integration health statuses match Phase 3 definitions and precedence.
- Token refresh attempts update persisted state and are auditable via logs + AuditInterceptor.
- needs_reauth blocks publishing and analytics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-observability/03-01-SUMMARY.md`
</output>
