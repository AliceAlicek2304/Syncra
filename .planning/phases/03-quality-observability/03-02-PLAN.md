---
phase: "03-quality-observability"
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: [
  "tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs",
  "tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs"
]
autonomous: true
requirements: ["3.2"]
must_haves:
  truths:
    - "AnalyticsController endpoints are covered by integration-style tests using WebApplicationFactory"
    - "AnalyticsController maps Result<T> successes to 200 and failures to 400 consistently"
    - "StripeWebhookController rejects invalid signatures with 400"
  artifacts:
    - path: "tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs"
      provides: "End-to-end controller tests for analytics endpoints"
---

<objective>
Complete the test suite for Analytics and Stripe webhooks to reduce regressions and ensure predictable API behavior.

Purpose: protect critical stability surfaces (analytics + billing webhooks).
Output: new `AnalyticsControllerTests` and an expanded Stripe webhook test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/Syncra.Api/Controllers/AnalyticsController.cs
@src/Syncra.Api/Common/ResultExtensions.cs
@src/Syncra.Api/Controllers/StripeWebhookController.cs
@tests/Syncra.UnitTests/Api/Middleware/TenantResolutionMiddlewareTests.cs
@tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnalyticsController integration-style tests</name>
  <files>tests/Syncra.UnitTests/Api/AnalyticsControllerTests.cs</files>
  <action>
    Create `AnalyticsControllerTests.cs` using `WebApplicationFactory<Program>` and `ConfigureTestServices`.

    Test harness requirements:
    - Reuse the existing test auth approach used by `TenantResolutionMiddlewareTests` (header-driven user id).
    - Register a mocked `IMediator` so controller requests can be verified and made deterministic.

    Test cases (minimum):
    - GET `/api/v1/workspaces/{workspaceId}/analytics/{integrationId}?date=30`
      - mediator returns Result.Success => 200
      - mediator returns Result.Failure => 400 with `{ error = ... }`

    - GET `/api/v1/workspaces/{workspaceId}/analytics/post/{postId}?date=30`
      - success => 200
      - failure => 400

    - GET `/api/v1/workspaces/{workspaceId}/analytics/summary?date=30`
      - success => 200
      - failure => 400

    - GET `/api/v1/workspaces/{workspaceId}/analytics/heatmap?date=90`
      - success => 200
      - failure => 400

    Ensure every test:
    - sets `X-Test-UserId` header (to satisfy [Authorize])
    - validates the IMediator request type + parameters
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~AnalyticsControllerTests</automated>
  </verify>
  <done>AnalyticsController tests are present and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend StripeWebhookControllerTests for invalid signature</name>
  <files>tests/Syncra.UnitTests/Api/StripeWebhookControllerTests.cs</files>
  <action>
    Add a new test to `StripeWebhookControllerTests`:
    - When `Stripe-Signature` is missing or invalid, `StripeWebhookController` returns 400.

    Keep existing idempotency tests unchanged.
  </action>
  <verify>
    <automated>dotnet test tests/Syncra.UnitTests --filter FullyQualifiedName~StripeWebhookControllerTests</automated>
  </verify>
  <done>Stripe webhook tests cover invalid signature path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Controllers must return stable and predictable status codes. |
| External -> Stripe webhook | Signature validation protects against forged webhooks. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | StripeWebhookController | mitigate | Tests enforce 400 on invalid signature to prevent accidental relaxation. |
</threat_model>

<verification>
- Run targeted controller tests.
- Run full test suite at end of phase.
</verification>

<success_criteria>
- AnalyticsController has coverage for all Result-mapped endpoints.
- StripeWebhookController invalid signature behavior is verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-observability/03-02-SUMMARY.md`
</output>
