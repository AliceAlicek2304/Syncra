---
phase: 10
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
autonomous: true
requirements:
  - REQ-8.5
files_modified:
  - fe/src/pages/app/CalendarPage.tsx
  - fe/src/pages/app/CalendarPage.module.css
  - fe/src/context/CalendarContext.tsx
objective: "Bind Calendar page to scheduling API with skeleton loading + optimistic reschedule/delete, without changing layout/interaction."
must_haves:
  truths:
    - "Must NOT change Calendar view switcher UI (Month/Week/Day)"
    - "Must NOT change Month grid layout + right-side detail panel"
    - "Drag-to-reschedule commits immediately on drop"
  acceptance:
    - "Month navigation triggers real API query (month-range)"
    - "Calendar surfaces show skeleton overlays while loading"
    - "Reschedule failure rolls back and shows toast: `Could not reschedule post. Please try again.`"
---

<objective>
CalendarPage uses real backend data (month-range query) and performs optimistic reschedule/delete.
</objective>

<context>
@.planning/phases/10-scheduling-analytics/10-UI-SPEC.md
@fe/src/pages/app/CalendarPage.tsx
@fe/src/context/CalendarContext.tsx
@fe/src/context/ToastContext.tsx
@fe/src/hooks/useCalendarPosts.ts
</context>

<tasks>

<task id="10.02.1" type="execute">
  <name>Remove mock post dependency and bind month data</name>
  <action>
    In `CalendarPage.tsx`:
    - Stop using `MOCK_POSTS` for day cells.
    - Use `useWorkspace()` to get `workspaceId` (consistent with other pages).
    - Use `useCalendarPosts({ workspaceId, year, month })` to fetch and derive `filteredPostsByKey`.

    Keep existing visual structure: header, toolbar, month grid, week/day view, detail panel.
  </action>
  <done>Calendar renders real posts from API for current month.</done>
</task>

<task id="10.02.2" type="execute">
  <name>Implement skeleton overlays (D-07)</name>
  <action>
    While query `isLoading` / `isFetching`:
    - Keep shell visible.
    - Render skeletons inside the calendar card surface (month grid / week grid / day grid) using `<Skeleton />`.

    Do not add spinners.
  </action>
  <done>Loading state feels premium and avoids layout shift.</done>
</task>

<task id="10.02.3" type="execute">
  <name>Optimistic reschedule on drag-drop with rollback</name>
  <action>
    On drop:
    - Compute target `scheduledAtUtc` using target local day + preserve time (or default time if missing).
    - Call `reschedule(postId, scheduledAtUtc)` mutation.
    - Update UI immediately (optimistic).
    - On API failure: rollback and toast exact copy:
      `Could not reschedule post. Please try again.`

    Add subtle `Saving…` micro-indicator while mutation in-flight:
    - text only
    - color: `--text-muted`
    - no spinner
  </action>
  <done>Drag-drop behaves identical but now persists to backend.</done>
</task>

<task id="10.02.4" type="execute">
  <name>CalendarContext adjustment (align with D-04)</name>
  <action>
    Refactor `CalendarContext` to stop persisting posts in localStorage. Options:
    - Minimal: keep context for UI-only state and move posts to `useCalendarPosts`.
    - If context currently required by other pages, keep API stable but deprecate storage.

    Goal: Calendar source of truth = backend + react-query cache.
  </action>
  <done>Calendar no longer diverges from backend due to localStorage state.</done>
</task>

</tasks>

<verification>

```bash
cd fe
npm run build

# manual UAT
# 1) Open /app/calendar, navigate months -> network calls happen
# 2) Drag a scheduled post to another day -> immediate UI move
# 3) Simulate API failure -> rollback + toast copy matches spec
```

</verification>
