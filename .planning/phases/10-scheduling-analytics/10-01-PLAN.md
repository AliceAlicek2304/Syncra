---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: ["09-01"]
autonomous: true
requirements:
  - REQ-8.5
  - REQ-8.6
files_modified:
  - fe/src/api/analytics.ts
  - fe/src/api/notifications.ts
  - fe/src/api/posts.ts
  - fe/src/hooks/useCalendarPosts.ts
  - fe/src/hooks/useAnalyticsSummary.ts
objective: "Create Phase 10 API modules + query hooks used by Calendar + Analytics + Notifications UI."
must_haves:
  truths:
    - "All new API modules use shared `api` Axios instance from fe/src/lib/axios.ts"
    - "No new npm deps in this plan"
    - "Analytics endpoints match AnalyticsController routes (summary + heatmap)"
  acceptance:
    - "fe/src/api/analytics.ts exports analyticsApi with getWorkspaceSummary(dateDays) + getWorkspaceHeatmap(dateDays)"
    - "fe/src/hooks/useAnalyticsSummary.ts exposes preset -> queries + computed range label"
    - "fe/src/hooks/useCalendarPosts.ts exposes month-range query + reschedule/delete mutations"
---

<objective>
Create typed API clients and React Query hooks for Phase 10 data binding.
</objective>

<context>
@.planning/phases/10-scheduling-analytics/10-CONTEXT.md
@.planning/phases/10-scheduling-analytics/10-UI-SPEC.md
@be/src/Syncra.Api/Controllers/AnalyticsController.cs
@be/src/Syncra.Api/Controllers/PostsController.cs
@be/src/Syncra.Application/DTOs/Analytics/WorkspaceAnalyticsSummaryDto.cs
@fe/src/lib/axios.ts
</context>

<tasks>

<task id="10.01.1" type="execute">
  <name>Create `fe/src/api/analytics.ts`</name>
  <action>
    Implement `analyticsApi`:
    - `getWorkspaceSummary(workspaceId: string, dateDays: 7|30|90)` -> GET `workspaces/${workspaceId}/analytics/summary?date=${dateDays}`
    - `getWorkspaceHeatmap(workspaceId: string, dateDays: 7|30|90)` -> GET `workspaces/${workspaceId}/analytics/heatmap?date=${dateDays}`

    Export types mirroring backend DTOs:
    - `WorkspaceAnalyticsSummaryDto`, `WeeklyReachDto`, `HeatmapDto`, `HeatmapSlotDto`
  </action>
  <done>Typed analytics API module exists and compiles.</done>
</task>

<task id="10.01.2" type="execute">
  <name>Extend `fe/src/api/posts.ts` for calendar range query + mutations</name>
  <action>
    Add functions (keep existing exports stable):
    - `getPosts(workspaceId, params: { status?: string; scheduledFromUtc?: string; scheduledToUtc?: string; page?: number; pageSize?: number })`
    - `deletePost(workspaceId, postId)`
    - Optional convenience: `reschedulePost(workspaceId, postId, scheduledAtUtc: string)` as wrapper around `updatePost`.

    Ensure params serialize as query string and match backend names: `scheduledFromUtc`, `scheduledToUtc`.
  </action>
  <done>Calendar can query month posts and mutate schedule/delete via postsApi.</done>
</task>

<task id="10.01.3" type="execute">
  <name>Create `fe/src/hooks/useCalendarPosts.ts`</name>
  <action>
    Create hook responsible for:
    - Computing month range in UTC for query (`scheduledFromUtc`, `scheduledToUtc`).
    - `useQuery` keyed by `["calendar-posts", workspaceId, year, month]`.
    - `useMutation` for reschedule + delete with optimistic update (react-query `queryClient.setQueryData`).
    - Expose `isLoading`, `isFetching`, `isMutating`, `posts`, `reschedule`, `remove`.

    Mapping rule (UI needs local calendar cells):
    - Convert `ScheduledAtUtc` (ISO) -> local `Date` before deriving (year, month, day, time).
  </action>
  <done>Calendar has single source of truth for month posts with optimistic writes.</done>
</task>

<task id="10.01.4" type="execute">
  <name>Create `fe/src/hooks/useAnalyticsSummary.ts`</name>
  <action>
    Hook responsibilities:
    - Manage preset state: `7|30|90` days (copy: Last 7/30/90 days).
    - `useQuery` for summary + heatmap keyed by `["analytics", workspaceId, presetDays]`.
    - Provide computed range label for UI (e.g. `Apr 01 – Apr 07, 2026`).
    - Provide loading state used to render skeleton overlays.
  </action>
  <done>Analytics page can bind all cards/charts to hook outputs.</done>
</task>

<task id="10.01.5" type="execute">
  <name>Create `fe/src/api/notifications.ts` (HTTP only; hub later)</name>
  <action>
    Create module with typed functions (backend endpoints implemented in Plan 10-04):
    - `listNotifications(workspaceId, params?: { unreadOnly?: boolean; take?: number })`
    - `markRead(workspaceId, notificationId)`
    - Optional: `markAllRead(workspaceId)`

    Keep API shape minimal and forward-compatible; do not add dependencies.
  </action>
  <done>Notifications UI can fetch list + persist read/unread once backend exists.</done>
</task>

</tasks>

<verification>

```bash
cd fe
npm run build

# smoke: exports exist
ls src/api/analytics.ts src/hooks/useCalendarPosts.ts src/hooks/useAnalyticsSummary.ts src/api/notifications.ts
```

</verification>
