---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: ["10-01"]
autonomous: true
requirements:
  - REQ-8.6
  - REQ-8.3
files_modified:
  - fe/src/pages/app/AnalyticsPage.tsx
  - fe/src/pages/app/AnalyticsPage.module.css
  - fe/src/components/Heatmap.tsx
  - fe/src/components/Heatmap.module.css
objective: "Bind Analytics page + Heatmap to backend metrics, add preset selector + skeleton states, implement 7×24 heatmap with local-time + tooltip."
must_haves:
  truths:
    - "Preset options copy exact: Last 7 days / Last 30 days / Last 90 days"
    - "Heatmap intensity uses relative max: intensity = score / maxScore, clamp [0,1]"
    - "Heatmap converts API UTC hour -> browser local hour before placing into 24 columns"
  acceptance:
    - "Analytics header shows preset selector + computed range label"
    - "All metric cards and weekly bars bind to WorkspaceAnalyticsSummaryDto"
    - "Heatmap renders 7×24 grid + tooltip `{Day}, {HH:mm} — {count} posts published`"
---

<objective>
Replace AnalyticsPage mock data with live metrics and implement spec-compliant heatmap.
</objective>

<context>
@.planning/phases/10-scheduling-analytics/10-UI-SPEC.md
@fe/src/pages/app/AnalyticsPage.tsx
@fe/src/components/Heatmap.tsx
@fe/src/hooks/useAnalyticsSummary.ts
@fe/src/components/Skeleton.tsx
@be/src/Syncra.Application/DTOs/Analytics/WorkspaceAnalyticsSummaryDto.cs
</context>

<tasks>

<task id="10.03.1" type="execute">
  <name>Analytics header preset selector (D-06)</name>
  <action>
    Replace static date badge with dropdown selector:
    - Pill button: calendar icon + selected label + chevron
    - Dropdown glass-card with 3 options (exact copy)
    - Selected option shows check + `--purple-300`

    Show computed range label as muted subtext (placement implementer choice).
    State driven by `useAnalyticsSummary`.
  </action>
  <done>Preset selector controls all analytics queries.</done>
</task>

<task id="10.03.2" type="execute">
  <name>Bind metric cards + weekly reach chart to summary DTO</name>
  <action>
    Use summary fields:
    - Total Reach -> `TotalReach`
    - Avg. Engagement -> `EngagementRate` (percent)
    - Follower Growth -> `FollowerGrowth`
    - Total Posts -> `TotalPosts`

    Weekly chart:
    - bind bars to `WeeklyReach[]`.
    - keep existing CSS bar layout.
  </action>
  <done>Analytics metrics reflect backend summary values.</done>
</task>

<task id="10.03.3" type="execute">
  <name>Loading skeletons (D-07)</name>
  <action>
    While summary query loading:
    - metric cards: skeleton number + delta line
    - weekly bars: skeleton bars matching positions
    - heatmap: skeleton overlay on grid
    - platform table: 6 skeleton rows

    Use `<Skeleton />`. No spinners.
  </action>
  <done>Loading states match UI-SPEC and feel "Pro Max".</done>
</task>

<task id="10.03.4" type="execute">
  <name>Heatmap 7×24 grid + local-time placement + tooltip</name>
  <action>
    Update `Heatmap.tsx` to:
    - Render rows Mon–Sun, columns 0–23.
    - X-axis tick labels every 3 hours: `00:00, 03:00, ... 21:00`.
    - Convert each API slot `{DayOfWeek, Hour(UTC), Score}` to local placement:
      - Convert `Hour` UTC -> local hour (browser timezone).
      - Day shift must be handled (UTC hour conversion can change day).

    Intensity:
    - `maxScore = max(slots.Score)`; if `<=0` -> intensity 0.
    - `intensity = clamp(score / maxScore, 0, 1)`.

    Colors:
    - 0 -> `rgba(255,255,255,0.03)`
    - >0 -> `rgba(139, 92, 246, 0.12 + intensity * 0.58)`
    - intensity >= 0.75 -> add subtle glow (`box-shadow: 0 0 0 1px var(--border-glow)`).

    Tooltip on hover/focus:
    - `{Day}, {HH:mm} — {count} posts published`
    - glass style (12px Inter, border `--border-glass`).
  </action>
  <done>Heatmap matches UI-SPEC visual + interaction contract.</done>
</task>

</tasks>

<verification>

```bash
cd fe
npm run build

# manual
# - Change preset 7/30/90: metrics + heatmap update
# - Hover heatmap cell: tooltip format exact
# - Verify 7x24 DOM grid exists
```

</verification>
