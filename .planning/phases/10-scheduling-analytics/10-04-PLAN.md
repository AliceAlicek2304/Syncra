---
phase: 10
plan: 04
type: execute
wave: 4
depends_on: ["10-01"]
autonomous: true
requirements:
  - REQ-8.3
files_modified:
  - be/src/Syncra.Api/Program.cs
  - be/src/Syncra.Api/DependencyInjection.cs
  - be/src/Syncra.Api/Hubs/NotificationHub.cs
  - be/src/Syncra.Api/Controllers/NotificationsController.cs
  - be/src/Syncra.Domain/Entities/Notification.cs
  - be/src/Syncra.Infrastructure/Persistence/AppDbContext.cs
  - be/src/Syncra.Infrastructure/Persistence/Configurations/NotificationConfiguration.cs
  - be/src/Syncra.Infrastructure/Migrations/*
  - fe/package.json
  - fe/src/hooks/useNotificationHub.ts
  - fe/src/components/NotificationBell.tsx
  - fe/src/components/NotificationBell.module.css
  - fe/src/pages/app/AppLayout.tsx
objective: "Implement persisted notifications + SignalR hub + global bell UI with unread dot + dropdown + optional toast."
must_haves:
  truths:
    - "Only new FE dependency allowed: `@microsoft/signalr`"
    - "Bell badge is dot only (no number)"
    - "Incoming realtime notification may also toast via useToast()"
  acceptance:
    - "Bell visible on all /app/* routes (AppLayout)"
    - "Unread dot shows when unread exists"
    - "Dropdown empty state copy exact"
    - "SignalR connects and receives notifications"
---

<objective>
Add real-time notification UX and backend delivery via SignalR, persisted in DB.
</objective>

<context>
@.planning/phases/10-scheduling-analytics/10-UI-SPEC.md
@be/src/Syncra.Api/Program.cs
@be/src/Syncra.Api/DependencyInjection.cs
@fe/src/pages/app/AppLayout.tsx
@fe/src/context/ToastContext.tsx
</context>

<tasks>

<task id="10.04.1" type="execute">
  <name>Backend: Notification entity + persistence</name>
  <action>
    Add `Notification` domain entity (workspace-scoped, optional user-scoped):
    - `Id: Guid`
    - `WorkspaceId: Guid`
    - `UserId?: Guid`
    - `Type: string`
    - `Title: string`
    - `Body: string`
    - `PayloadJson?: string`
    - `CreatedAtUtc: DateTimeOffset`
    - `ReadAtUtc?: DateTimeOffset`

    Add EF configuration + DbSet + migration.
  </action>
  <done>Notifications persisted; read/unread stored server-side.</done>
</task>

<task id="10.04.2" type="execute">
  <name>Backend: NotificationsController (list + mark read)</name>
  <action>
    Add controller under `/api/v1/workspaces/{workspaceId}/notifications`:
    - `GET` list (default sort newest first, allow `unreadOnly`, `take`).
    - `POST /{notificationId}/read` (or `PUT`) sets `ReadAtUtc`.
    - Optional: `POST /read-all`.

    Keep payload minimal for FE: id, type, title, body, createdAtUtc, readAtUtc.
  </action>
  <done>FE can fetch + persist read/unread state.</done>
</task>

<task id="10.04.3" type="execute">
  <name>Backend: SignalR hub setup</name>
  <action>
    Add SignalR:
    - `services.AddSignalR()` in API DI.
    - `app.MapHub<NotificationHub>("/api/v1/hubs/notifications")`.

    JWT for hub:
    - Configure JwtBearer `OnMessageReceived` to accept `access_token` query string for hub route.

    Hub behavior:
    - On connect, join group `workspace:{workspaceId}`.
    - Broadcast server-side event name: `notification` with notification DTO payload.
  </action>
  <done>Authenticated websocket connection receives notification payloads.</done>
</task>

<task id="10.04.4" type="execute">
  <name>Backend: emit notifications from at least one real event</name>
  <action>
    Minimal MVP emitter:
    - When post publish succeeds/fails (PublishService path), create Notification row and broadcast to workspace group.

    Copy requirements for toast strings (FE-level):
    - success: `Post published successfully.`
    - failure: `Post failed to publish. Check details and try again.`
  </action>
  <done>Realtime notifications demonstrably flow from backend to UI.</done>
</task>

<task id="10.04.5" type="execute">
  <name>Frontend: add `@microsoft/signalr` + `useNotificationHub` hook</name>
  <action>
    - Add dependency `@microsoft/signalr`.
    - Implement `useNotificationHub`:
      - connect/reconnect
      - subscribe to `notification` event
      - update query cache for notifications list
      - optionally trigger `useToast().success/error` based on type
  </action>
  <done>FE can receive + react to realtime events.</done>
</task>

<task id="10.04.6" type="execute">
  <name>Frontend: NotificationBell component + AppLayout placement</name>
  <action>
    Create `NotificationBell`:
    - 40×40 hit area, bg `rgba(255,255,255,0.04)`, border `--border-glass`, hover border `--purple-400`.
    - Unread badge dot 8px, color `--pink-500`.
    - Dropdown glass-card width 320 desktop.

    Copy (exact):
    - Title: `Notifications`
    - Empty heading: `No notifications yet`
    - Empty body: `We'll let you know when something important happens.`

    Place inside `AppLayout` so visible on all `/app/*` routes.
  </action>
  <done>Bell UX matches UI-SPEC.</done>
</task>

</tasks>

<verification>

```bash
# backend
cd be
dotnet test

# frontend
cd fe
npm run build

# manual
# 1) Open any /app/* route: bell visible
# 2) Trigger notification (publish post): unread dot appears, dropdown shows item
# 3) Realtime toast shows (optional), mark read persists after refresh
```

</verification>
